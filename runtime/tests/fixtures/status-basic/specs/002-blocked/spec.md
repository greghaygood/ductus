---
status: planned
dependencies: [000-blocker]
tags: [pipeline]
---

# 002 — Blocked Fixture

A planned spec that depends on `000-blocker` (status `draft`). The
dashboard reports it as blocked: its `blocked-by` array contains
`000-blocker`. Exercises the per-spec `blocked-by` computation and the
"Blocked" callout in the rendered view.

## Open Questions

*None — all resolved.*
