# Inbox

<!-- Rules:
     - Do not frontfill bugs that are not being actively worked on.
     - A bug or omission inside the scope of the spec currently in progress does NOT belong
       here — it becomes a task on that spec's tasks.md. The inbox is for findings with no
       home yet; an in-progress spec is already the home, so an item logged here is routed
       straight back to it (constitution §brownfield-inbox, scope decides the destination).
     - Write specs for areas being actively touched — let adoption spread naturally.
     - As specs are written, items migrate from here into spec updates or new scenarios.
     - Chores (project maintenance with no feature home — lint/formatting cleanup,
       dependency cleanup, repo hygiene) also live here; /groom recognizes them and leaves
       them in place. They clear when done, not by migrating to a spec.
     - The brownfield backlog drains toward empty as adoption completes; incidental
       capture is ongoing, so the file persists while work keeps surfacing issues.
     - Status notes do NOT belong here. Every item must be routable by /groom to one of
       its five routes (rule, spec, scenario, chore, discard); a "where things stand"
       or "what to do next" note matches none of them, so it would be walked and
       re-discarded on every pass forever. Pipeline state is derived — read it from
       /status, tasks.md, and git — not narrated into the backlog.

     Format each item as a checkbox list entry with a brief description and any relevant
     context. Three forms are in use:

     1. Manual entry (via /log) — the simple form below:
        `- [ ] {Brief description of the issue and any relevant context}`

     2. Auto-captured finding (an agent recorded this automatically while working a task,
        per §brownfield-inbox Automatic issue capture). Lead with a category so /groom can
        route it, and include a source pointer:
        `- [ ] {category}: {summary} — {file:line or area} (captured during {NNN-feature})`
        Categories: security, leak (memory/resource), convention, bug, perf, other.
        Security issues and leaks are the highest-priority captures.

     3. Audit finding written by /ductus — stricter form (see
        specs/008-security-rules/spec.md): `- [ ] {Rule ID}: {artifact} does not address — {summary}`.

     When an item is migrated, remove it from this list. -->

- [ ] convention: grounding — a `done` spec corpus, two rule files and the runtime record another adopter project's name, against §Workflow's "never record another project's name in this repository — describe the shape instead". Measured 2026-09-13: ~100 occurrences. They split three ways and only the first is a straight sweep. (1) **Spec prose** — `000` (spec + plan, 6 hits), `001` (spec + plan, 3), `002` (spec + plan + tasks, 5), `017` (2 scenarios), `022` (6 scenarios), `036` (spec + plan + tasks, 4): each states *what kind of project* it derived from or was motivated by, so the name drops out with no loss of meaning. (2) **`framework/rules/quality-cross.md`** `QUAL-STUB-001`'s Rationale and `framework/migrations/{ductus-rename,session-file-consolidate}.md` — these **ship to adopters**, which makes the leak worse than in a spec body. (3) **`runtime/`** — source doc comments, `CHANGELOG.md`, and test fixtures that use the name as a *placeholder project* (`tests/fixtures/exec-auggie/`, `exec-opencode/`, `parity.rs`, `host.rs`): a fixture is the sanctioned form per that same entry, so whether these count is an operator call, and changing them is a `runtime/` edit that needs the version bump and `ductus-v<version>` tag. Note the entry's own warning: the replacement text varies per sentence, so this is **not** a uniform substitution and does not earn the mechanical-sweep exemption — each affected spec takes the back-edge (captured during /ductus:analyze of 001). **Tier (2) is DONE (2026-09-13)** — the shipped half, which the item itself called the worse leak: `framework/rules/quality-cross.md` `QUAL-STUB-001`'s Rationale now says *an adopter project running a backend service*, `framework/migrations/ductus-rename.md` says `/{project}:specify`, and `framework/migrations/session-file-consolidate.md`'s two examples use a placeholder `acme`. a case-insensitive `git grep` for the adopter name over `framework/` is now empty, so nothing adopters receive carries it. **Tier (1) remains; tiers (2) and (3) are done and verified empty.** **Re-measured 2026-09-14 during grooming, because the tier list above had decayed in both directions.** Tier (1) is **27 occurrences across six closed specs**: the first name in `001` (3), `002` (6), `017` (2), `022` (10) and `036` (4), and a **second adopter name** — folded in from a separate item on 2026-09-13 — in `022/scenarios/derive-references-unstaged-drift-is-reported.md` (1) and `046/spec.md` (1). `000`'s and `045`'s shares are **discharged** in their own passes, so the six-spec list this item used to carry named one spec that is clean and missed `046`'s durable-contract hit. **Disposal under the two-disposal rule** (`AGENTS.md` §Workflow): there is no not-a-spec home for prose *inside* a spec, so tier (1) is spend-the-reopen — and **022's eleven were discharged in the 2026-09-14 batch reopen**, leaving `001`, `002`, `017`, `036` and `046` as five deliberate reopens. **That pass also surfaced a THIRD project name this item never measured** — a second real local project, spelled once in `022/scenarios/commands-dir-parameterization.md` as an illustrative namespace beside the first. Both are now neutral placeholders there. Re-grep for *any* real project name rather than the two this item enumerates; a token list is the wrong instrument, as 023's AC21 already demonstrated at 202 occurrences, of which `022`'s and `046`'s hits are the durable-contract kind that cost a five-pass re-review rather than an analyze. **Tier (3) is DONE — shipped as `ductus-v0.49.5` on 2026-09-14, and the operator call it was waiting on was made: rename the placeholder rather than keep it.** It measured 83 occurrences (80 lowercase, 3 capitalized) across 9 `runtime/src/` files, 8 `runtime/tests/` files, `runtime/CHANGELOG.md` and `scripts/audit/runtime-hardcoded-paths.sh` — that last one outside `runtime/` entirely and missed by the tier list above. **The finding that decided it, and the reason this was not the internal-only cleanup the tier description implied:** the occurrences were not one kind of thing. Most were the name as an arbitrary value for the `project` config key, where any string works identically — but six named the adopter *as an adopter* in prose, and one of those was a doc comment on a `clap::Args` field, so `ductus migrate-session-file --help` **printed a real adopter's name from the shipped binary**. Proven by probe before and after, not inferred. Those two groups are not independent: the attributing site is what stopped the rest from reading as arbitrary, which is why renaming the placeholder to `acme` and rewriting the prose to describe the shape had to happen together. The two fixture command directories moved with `git mv` and every reference moved in the same commit — including two `CHANGELOG.md` entries citing the fixture path, which would otherwise have been left pointing at a directory that no longer exists. **The second adopter name has been stripped from this item** (2026-09-14): spelling it out here was the rule's own violation sitting in the file that reports it, and the inbox is not a spec artifact, so removing it reopened nothing and staled no review. Both names are merged into this one item with the full spread rather than carried separately and rediscovered twice. **Re-derived 2026-09-15 while spec 056 was open, and TWO of this item's own claims above are wrong — do not act on them.** Tier (1) is **19 occurrences across SIX specs, not 16 across five**. `anvil` x17: `001` (spec 1, plan 3), `002` (spec 1, plan 3, tasks 2), `017` (3, across `scenarios/detect-dependency-cycles.md` and `scenarios/skip-prose-cross-references.md`), `036` (spec 2, plan 1, tasks 1). `svc-zmc-api` x2: `022/scenarios/derive-references-unstaged-drift-is-reported.md:10` and `046/spec.md:51`. **(a) 022 is NOT discharged.** This item states its eleven were all taken in the 2026-09-14 batch reopen; one `svc-zmc-api` survives in that scenario, so 022 is a sixth reopen and a durable-contract one. **(b) 046's hit is in `spec.md`, not a durable contract** — this item calls 046's "the durable-contract kind", which is false; the durable-contract hits are 017's and 022's. So the disposal is **six reopens: four `analyze-stale` (001, 002, 036, 046) and two `review-stale` (017, 022)**, not the five this item describes, and the expensive one is 022 rather than 046. Derived by `git grep -ioE` per file and confirmed against `specs/046-scenario-open-question-visibility/spec.md:51`; a corpus sweep for further real project names surfaced none beyond the three already known (the third, a local project name, was already neutralised).
- [ ] convention: second promotion round — audit `AGENTS.md` against `framework/constitution.md` and move everything adopter-beneficial into the constitution, consolidating anything stated in both to the constitution with an `AGENTS.md` link. **Operator request 2026-09-13**, stated as two halves: (a) everything currently in `AGENTS.md` that can benefit adopters moves to `framework/constitution.md`, which is the one artifact adopters receive; (b) any instruction living in *both* is consolidated into the constitution, with `AGENTS.md` keeping a link. **Measured 2026-09-13.** `AGENTS.md` is 148 lines / 135,952 bytes carrying 103 top-level bullets: 92 rule-bearing across the four sections 050's plan scopes (Workflow 44, Gotchas 40, Boundaries 3, Design Principles 5) plus 11 descriptive (Project Structure 6, Tech Stack 5) that 050 correctly held out of scope. **41 entries cite a `framework/constitution.md#` anchor; 62 cite none.** Of the 41, only 31 use the established mirror phrasing (26 "contributor-side mirror", 5 "contributor-side record"), so roughly 10 cite the constitution without declaring themselves mirrors — which is exactly where half (b)'s duplication hides, since a restatement that happens to link is indistinguishable from a pointer by grep. The constitution is 760 lines across 32 anchored sections. **This is 050's territory and needs no new method.** §drift-prevention's canonical-sources map names `specs/050-constitution/spec.md` as the home for constitution-content work — "what belongs in it, how it is organized, which rules adopters receive" — and 050 already supplies every instrument this needs: the **reword test** (promote iff the rule restates without naming machinery only this repo has *and* without losing what makes it actionable), the three-tier **Classification** (universal → constitution; shared → an org's registered `[constitutions.*]` document per 055; project-only → stays), and **§Promotion mechanism** (one normative statement in the constitution, one `AGENTS.md` line that points and states nothing of its own — which is half (b) exactly). 050's plan already anticipates this run: "the table is a snapshot of one pass, and a later promotion round re-derives rather than amends it." **Root cause — do the flow fix first, before the 92-entry pass.** The backlog is not neglect and 050's AC1 did not cause it: AC1 correctly rejected a per-entry marker, since a field an author must remember to fill is the diligence dependency §design-principles rejects. The cause is that **every write-time routing rule discriminates by *kind*, while promotion discriminates by *population*.** Three sites tell an author where a learning goes and none asks the universality question: `AGENTS.md:5`'s header says only "append durable learnings to the matching section"; `CLAUDE.md`'s Auto-Memory Routing asks "would this help any other contributor **to this project**?"; and the constitution's §drift-prevention *Shared knowledge stays in git* answers by kind — *"A **project learning** — a convention, a gotcha, a workflow rule, a boundary — goes in `AGENTS.md`."* So a **universal** gotcha lands in `AGENTS.md` **because it is a gotcha**, not because anyone judged it project-only, and the population test that would catch it (050 §Classification: "one question about the rule's population") lives in a spec nobody reads while writing one. The default manufactures the backlog continuously — 54 rule-bearing entries at the survey, **92** now — so running the 92-entry pass without changing the routing just resets a counter that refills. **The fix is not a new authored input**, which is why §design-principles does not bar it: an author already chooses a destination, and changing *which question decides it* adds no remembered step. Move 050's population question into §drift-prevention's *Shared knowledge stays in git* bullets, and mirror it at `AGENTS.md:5` and in `CLAUDE.md`'s routing block — three small edits plus one 050 back-edge, landing **before** the bulk pass. **Correction to the counts above, which are proxies rather than measures.** Neither grep is the answer: the 62 non-citing entries **overcount** the backlog because 28 are settled project-only from 050's pass, and the 41 citing entries **overcount** the promoted set because project-only entries cite `§spec-lifecycle` and friends for context — 41 against the 26 that pass actually produced. A sound coverage check needs 050's classification table as an input, **re-keyed from positional IDs (`W4`, `G11`) to each entry's bolded lead phrase**, since positional IDs drift the moment an entry is inserted and the table is already stale by that mechanism alone. Then *unclassified = rule-bearing − (table-keyed ∪ constitution-citing)*, a new entry is unclassified **by default** (the honest state, and the opposite of a field that silently stays empty), and the number today is **~34**, not 62. Render it as a standing notice on the §brownfield-inbox inbox-row precedent — **notice, never gate**, because promotion has to stay free or the honest choice between a growing backlog and a silent one pushes toward silence. The re-keying is work this round must do anyway. **Honest limit on both halves:** neither makes promotion happen; one stops new misrouting and the other makes not-having-promoted visible. The reword test stays human judgment, which §runtime-boundary principle 2 keeps off the runtime regardless. **Scope note for whoever takes it:** this is a spec cycle of its own, not a session tail — 92 entries judged individually, each promotion reworded for adopter-neutrality with its reason recorded (AC1's audit trail), and every promoted entry's `AGENTS.md` line rewritten to a pointer. Expect the constitution to grow materially, so §Resolved Questions' bullets-not-sections rule and AC10's anchor-stability constraint both bind. Two constraints already settled and not to be re-litigated: a constitution-only change moves no version pin and needs no tag (050 §Resolved Questions), and the mirror stays in `AGENTS.md` rather than being deleted, because contributors read it as the index of how to work here and a rule that silently left reads as a rule that was dropped. **Routed 2026-09-14: a 050 back-edge, in two units and in the order this item already specifies — the root-cause flow fix first, then the bulk pass as its own spec cycle rather than a session tail.** **Every count above is superseded; re-measured the same day, and the decay is this item's own thesis borne out at the resolution of hours.** `AGENTS.md` is now **164 lines / 204,757 bytes** carrying **117** top-level bullets — **106 rule-bearing** across the four in-scope sections (Workflow 48, Gotchas 48, Boundaries 3, Design Principles 7) plus the same 11 descriptive ones 050 held out of scope — with **47** citing a `framework/constitution.md#` anchor and **32** using the declared-mirror phrasing. Against the 148 lines / 135,952 bytes / 103 bullets / **92** rule-bearing / 41 citing / 31 mirrors recorded above, the backlog grew **92 → 106 in a single day** and the file grew ~69KB. That is the item's own root-cause claim demonstrated rather than argued: the write-time default routes by *kind* and refills the queue faster than any bulk pass can drain it, so **running the bulk pass without the flow fix first resets a counter that refills**, and the ordering is not a preference. The corollary for whoever takes it: **do not promote entries opportunistically** while doing other work — a partial pass leaves the unclassified set unmeasurable, which is the state the standing-notice half exists to end. Re-derive the counts again at the start of the pass; the ones in this paragraph are evidence about 2026-09-14 and nothing more. **Re-derived 2026-09-15 and the queue grew again: 115 rule-bearing bullets (Workflow 49, Gotchas 55, Boundaries 3, Design Principles 8) across 177 lines / 220,627 bytes, with 49 citing a `framework/constitution.md#` anchor and 32 using the declared-mirror phrasing.** Against the 106 / 47 / 32 recorded above that is **+9 in a day**, and the split is the useful half: only **five** came from the `/ductus:init` retirement pass, so the other four landed from ordinary work in between. Nothing was promoted in that window. This is the item's own root-cause claim measured a second time — 92 → 106 → 115 while the promoted set moved by zero — and it is the argument for doing the flow fix first rather than a reason to hurry the bulk pass. Count with the same method before quoting these: top-level list bullets tallied per level-two heading, in-scope sections only. **2026-09-15: +1 from spec 056's pass — 116 rule-bearing, not 115.** One Gotchas entry was added (a recorded measurement can encode a constraint you have not found yet). It is flagged here rather than left for the counter to discover because it is a **universal-population** entry and therefore a promotion candidate on this item's own reword test: it was filed in `AGENTS.md` per the current write-time routing, not promoted opportunistically, which is exactly the default this item identifies as the root cause. Whoever runs the bulk pass should expect it in the unclassified set
- [ ] convention: **8 `§` reference occurrences remain unresolved on the durable tier** — down from 120 across four tiers; the `plan.md` tier is done, two tiers are discarded, and 022's share was swept in its 2026-09-14 reopen. Original measurement: **120 occurrences (95 distinct anchor names) outside `spec.md` had never resolved — re-measured 2026-09-14, against the 62 this item opened with,** and the sweep that fixed the `spec.md` tier was recorded as corpus-wide. **Measured 2026-09-13** by running `ductus resolve-anchor --markers-path framework/constitution.md` over every tracked spec artifact at the close of 029's pass: all 54 `spec.md` files report `unresolved: []`, and **62 references outside them do not**. A `§X` reference resolves by LINE — it counts as `qualified` only when the same line names a markdown document other than the markers file — so each of these reads as a claim about the constitution, where the named section does not exist. **The 62 was wrong twice and the correction is the item's most useful half.** It counted only two destinations — `tasks.md` and `review.md` had never been scanned — and it read `resolve-anchor`'s `unresolved` field, which is a **deduplicated set of anchor names** rather than a list of occurrences, so it understated the repair even within its own scope. Filter `references` on `resolved: false` for the real list; 048's pass was handed "8 unresolved" and found **12** lines, because one file named `§Runtime acquisition` four times. **The cost split by destination, re-measured 2026-09-14, is the whole planning input:** **19 occurrences across 11 durable-contract files** (`scenarios/*.md` and `data-model.md`) — the only tier that stales a `reviewed-digest` and therefore obliges a five-pass re-review; **28 across 13 `plan.md` files**, which `write_review.rs` does not digest, so that half reopens nothing and is free; **57 across 17 `tasks.md` files**; and **16 across 12 `review.md` files**. **The last two are close to worthless to repair and should probably be excluded outright**: `tasks.md` is ephemeral and `/{project}:prune`-able by construction, and `review.md` is regenerated wholesale by the next `write-review`, so a reference fixed there is destroyed the next time that spec is reviewed — which, for any spec the backfill campaign still touches, is soon. Deciding that is a routing call, not this item's to make; what the item now carries is the measurement that lets someone make it. **All four durable-side specs that the `examined`/`scope` backfill campaign still had open when this was written have since discharged their share inside their own passes** — 045 (one, `§Behavior` in its `data-model.md`), 013 (three), 023 (two, with two in `tasks.md` deliberately left) and 048 (twelve occurrences across four files, 2026-09-14) — exactly as 029 swept its own 14 plus 2 at no marginal cost. **026's one — `§Future` in its `plan.md` — is discharged** (2026-09-14, in its own backfill pass; the pointer named a `## Future Considerations` section removed when Family 9 was adopted, so the repair was to correct the claim rather than to make the anchor resolve). No spec on the durable tier is now waiting on a scheduled pass, because the campaign has none left. Nothing else on the durable tier belongs to a spec the campaign has yet to open. That leaves six closed specs (003, 022, 027, 028, 032, 050) whose share needs a deliberate reopen under the two-disposal rule in `AGENTS.md` §Workflow — and 022's is the expensive one, per the same reasoning that made its validation fix its own unit. **Routed and partly discharged 2026-09-14; all four tiers re-measured rather than quoted, and the total is 119 occurrences, not 120.** **(1) The `plan.md` tier is DONE** — it measured **27 across 12 files**, of which **22 were pure qualification repairs** (name the owning document on the reference's own line) and were applied and verified in the grooming pass, taking `plan.md` from 27 unresolved to **5**. Those 22 are mechanical under [§spec-lifecycle](../framework/constitution.md#spec-lifecycle) — the claim does not move, only the line names the document it always meant — so **no spec reopened and no `reviewed-digest` staled**. **The 5 left are named with reasons rather than left silent, because three of them are not qualification repairs at all**: `024/plan.md:20`, `025/plan.md:83` and `033/plan.md:37` each cite a **`§Behavior`** section, and no `framework/commands/*.md` file carries one any more (verified — the commands were restructured into `## Instructions` plus a `## Markdown-only reference`), so the target is *gone* and the repair is a factual **correction of the claim**, which takes the back-edge on each of those three specs; `027/plan.md:21` sits inside a fenced TOML block as illustrative code; and `043/plan.md:33` is a **quotation of text the spec deleted**, so making it resolve would destroy the record it exists to hold. **(2) The `tasks.md` (57 across 17) and `review.md` (16 across 12) tiers are DISCARDED**, which this item's own analysis already argued for and left to a routing call: `tasks.md` is ephemeral and `/{project}:prune`-able by construction, and `review.md` is regenerated wholesale by the next `write-review`, so a reference fixed there is destroyed by the next review of that spec. Neither passes the bar — *would you stop and fix it today* — and [§brownfield-inbox](../framework/constitution.md#brownfield-inbox) has no hold state, so they are discarded rather than carried. **(3) The durable tier is the only part that survives as work, and it is now 8 occurrences, not 19.** It measured 19 across 11 files in six closed specs (003, 022, 027, 028, 032, 050); **022 held 11 of them across six scenarios and all 11 were discharged in the 2026-09-14 batch reopen**, at no marginal cost inside a pass that re-reviewed those contracts anyway — which is the ride-along the two-disposal rule recommends. One of the 11 was not a reference at all but a generic `§name` standing for *a section name*, reworded rather than resolved. The remaining **8 across 003, 027, 028, 032 and 050** each need their own deliberate reopen. **Re-verified 2026-09-15 and unchanged at 8** — `003/scenarios/curl-sh-installer.md` (1), `027/scenarios/migration-chain-reference-integrity.md` (3), `028/data-model.md` (1), `032/data-model.md` (1), `050/scenarios/a-partial-read-is-not-a-read.md` (2). Worth stating because **003 was reopened and re-closed that same day** for the `/ductus:init` retirement and its share was *not* discharged there: that pass edited `spec.md` only, so the natural assumption that a recent reopen swept this along is wrong. Note also that all five files are `scenarios/*.md` or `data-model.md`, so every one of the five reopens costs a **full five-pass re-review** rather than a reopen plus an analyze — there is no cheap half left in this item. **The second half of this item is the entry, not the references.** `AGENTS.md` §Gotchas described that sweep as **corpus-wide** when it was `spec.md`-scoped, so a reader checking whether the class was handled would have concluded yes — the "a sweep's own enumeration is itself a claim" failure the same file teaches two entries away, committed by the entry that teaches it. Corrected in place on 2026-09-13 with the measurement above; recorded here because the correction is what makes the 62 visible, and nothing else would have surfaced them. (captured during /ductus:review of 029-bootstrap-runtime-autowire) **Re-verified 2026-09-15 at 8 occurrences — unchanged in count, but TWO of the five paths this item names do not exist.** `028-per-agent-mcp-registration` and `032-opencode-agent-support` are stale directory names; the real ones are `028-antigravity-agent` and `032-opencode-agent`. The live list, each re-run through `resolve-anchor --markers-path framework/constitution.md`: `003/scenarios/curl-sh-installer.md` (1), `027/scenarios/migration-chain-reference-integrity.md` (3), `028-antigravity-agent/data-model.md` (1), `032-opencode-agent/data-model.md` (1), `050/scenarios/a-partial-read-is-not-a-read.md` (2). All five are `scenarios/*.md` or `data-model.md`, so each reopen is a full five-pass re-review, as this item already says. **And a cheaper repair exists than qualification, proven by probe 2026-09-15: `resolve-anchor` skips inline code spans.** A plain `§Widget Thing` is reported; a backticked one is not. So where a reference names a section of a document that is *not* the constitution, backticking it states the truth (it is a section name, not a constitution anchor) and silences the false claim, without needing the document named on the same line. Spec 056 used this to take 25 unresolved names to zero across its own `spec.md` and `plan.md`, matching longest-first against the real heading list rather than by token — a token-wise first attempt closed no bold and produced the unbalanced markup Family 25 exists to catch.
- [ ] bug: 050's retired-filename sweep (`510eb25c`) inverted at least two legacy-describing records, and 9 further candidates are unread. **The shape**: the sweep substituted the current name into a sentence whose subject is the *retired* state, so the sentence still parses and asserts the opposite of what it recorded. `AGENTS.md` §Gotchas prescribes grepping a sweep diff for `X → X`; that cannot see this, because the two sides genuinely differ. **Two confirmed and fixed 2026-09-14**, both during 048's backfill pass: `specs/048-govern-acquired-runtime/scenarios/retired-namespace-tools-are-off-limits.md` read *a pre-`.ductus/` binary resolves `.ductus/`* (self-contradictory; the shipped `framework/bootstrap/ductus.md` still read `.govern/` and was correct throughout), and `specs/026-framework-self-audit/scenarios/family-23-sweep-target-manifest-parity.md` read *`.ductus/scripts/lib/specs-root.sh` kept resolving `.ductus/config.toml` then the legacy root with no `.ductus/` tier* — inverting the incident the scenario exists to record, which `AGENTS.md` carries correctly. **How the second was found is the durable half**: fixing the first removed one substitution from the sweep, which costs `mechanical_sweep`'s **repo-wide** uniformity test, and Family 19 immediately reported 026's review stale — a true signal the exemption had been masking. Proven by probe (Family 19 exits 0 at `9188d865`, 1 at `8d660cb3` in a detached worktree). Restoring 026's line returned it to `9dfa1299`, exactly its recorded `reviewed-digest`, so the gate went green with no review re-run and no claimed pass; both repairs are mechanical under [§spec-lifecycle](../framework/constitution.md#spec-lifecycle)'s *sweep residue where a substitution landed in a sentence it did not fit*, so neither spec left `done` for it. **Measured, not estimated**: `510eb25c` made 319 substitutions across 60 files; an aligned per-file diff filtered to pairs that removed the *last* retired token from a sentence carrying a legacy marker (`kept resolving`, `used to`, `pre-0N`, `legacy root`, `historical root`, `no longer exists`, `was the pre-`) yields **11 candidates across 8 files** — the two above plus 9 unread in `specs/005-workflows/spec.md` (now consolidated into 043), `specs/017-derive-dont-ask/{spec.md,data-model.md}`, `specs/022-deterministic-runtime/data-model.md` (its *historical roots* bullet is the strongest remaining candidate), `specs/027-bootstrap-migration-registry/spec.md` (three, where the dropped text is a `**superseded naming**` annotation rather than an inversion — possibly 050 acting deliberately), and `specs/029-bootstrap-runtime-autowire/scenarios/project-inputs-asked-once.md`. Each needs reading in full context; the filter is a candidate generator, not a verdict. **And the hunt is scoped too narrowly: 049's `9da4a7ae` produced the same shape and has never been run through the generator.** Found 2026-09-14 in 026's pass — `scenarios/host-namespace-parity.md` records a mismatch between the resolved namespace and the installed one, and 049 mapped **both** `govern` and `gov` to `ductus`, collapsing the two sides onto one name so the sentence asserted that `/ductus:*` names a namespace that does not exist while `.claude/commands/ductus/` sat beside it. Repaired in that pass. The generalisation, worth more than the instance: **a many-to-one substitution table is the tell** — a sweep that sends two distinct old tokens to one new token can flatten any sentence whose point is that the two differed, and the `X → X` grep cannot see it because neither side is unchanged. Re-run the generator over `9da4a7ae` as well as `510eb25c` before calling this item measured. **Cost by destination, which is what sets this**: every remaining candidate sits in a spec the corpus has already closed, so the two-disposal rule in `AGENTS.md` §Workflow binds — and four of the eight files are durable contracts (`data-model.md` / `scenarios/*.md`), each costing a reopen **plus a full five-pass re-review**, with 022 the corpus's largest. The `spec.md` hits reopen their spec but stale no review. Route with the other 022-blocked items rather than singly. **Not a mechanical sweep to run blind**: the correct repair differs per line, and the wrong one destroys the record a second time. **Routed 2026-09-14, and the unread count is 8, not 9** — one of the named candidate files, `specs/005-workflows/spec.md`, no longer exists, because 005 was consolidated into 043 on 2026-09-13; verified absent rather than assumed. **Disposal per destination, under the two-disposal rule in `AGENTS.md` §Workflow**: the strongest remaining candidate was `022/data-model.md`'s *historical roots* bullet and it is now **discharged** — read in the 2026-09-14 batch reopen and **confirmed as a real inversion**, bringing the confirmed count to four. It read `scripts/` → `.ductus/scripts/` → `.ductus/scripts/`; the pre-sweep bytes show the middle stage was `.govern/scripts/`, and the sentence's whole subject is that the chain had multiple roots. Unlike the first two, this one **is** visible to the prescribed `X → X` grep, which is worth knowing: the class has both shapes, so run the grep *and* the generator. That leaves **7 unread**; `017/spec.md` and `027/spec.md` (three) reopen their spec but stale **no** review, so each is a reopen plus an analyze; and `017/data-model.md` plus `029/scenarios/project-inputs-asked-once.md` are durable contracts costing a reopen **plus a full five-pass re-review** each, which is what makes them worth sequencing against any other reason to open those two specs. **The half most likely to be lost, so it is stated last and plainly: the generator has still never been run over `9da4a7ae`.** 049's sweep produced a confirmed instance of this exact shape and is a **many-to-one** substitution table (`govern` and `gov` both to `ductus`), which is the tell — a sweep that sends two distinct old tokens to one new token can flatten any sentence whose point is that the two differed, and neither the `X → X` grep nor this item's own filter sees it. Run the generator over `9da4a7ae` as well as `510eb25c` before calling this item measured (captured during /ductus:review of 048-govern-acquired-runtime) **Sized 2026-09-15 so the unrun half can be priced: `9da4a7ae` is 389 files changed / 4,110 insertions / 4,003 deletions, against `510eb25c`'s 70 files / 319 substitutions.** An order of magnitude wider, so the generator run over it is its own unit of work rather than a tail on the seven unread candidates — and it is the half with a confirmed instance already found by hand (`026/scenarios/host-namespace-parity.md`), so it is not speculative.
