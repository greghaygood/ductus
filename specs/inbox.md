# Inbox

<!-- Rules:
     - Do not frontfill bugs that are not being actively worked on.
     - A bug or omission inside the scope of the spec currently in progress does NOT belong
       here — it becomes a task on that spec's tasks.md. The inbox is for findings with no
       home yet; an in-progress spec is already the home, so an item logged here is routed
       straight back to it (constitution §brownfield-inbox, scope decides the destination).
     - Write specs for areas being actively touched — let adoption spread naturally.
     - As specs are written, items migrate from here into spec updates or new scenarios.
     - Chores (project maintenance with no feature home — lint/formatting cleanup,
       dependency cleanup, repo hygiene) also live here; /groom recognizes them and leaves
       them in place. They clear when done, not by migrating to a spec.
     - The brownfield backlog drains toward empty as adoption completes; incidental
       capture is ongoing, so the file persists while work keeps surfacing issues.
     - Status notes do NOT belong here. Every item must be routable by /groom to one of
       its five routes (rule, spec, scenario, chore, discard); a "where things stand"
       or "what to do next" note matches none of them, so it would be walked and
       re-discarded on every pass forever. Pipeline state is derived — read it from
       /status, tasks.md, and git — not narrated into the backlog.

     Format each item as a checkbox list entry with a brief description and any relevant
     context. Three forms are in use:

     1. Manual entry (via /log) — the simple form below:
        `- [ ] {Brief description of the issue and any relevant context}`

     2. Auto-captured finding (an agent recorded this automatically while working a task,
        per §brownfield-inbox Automatic issue capture). Lead with a category so /groom can
        route it, and include a source pointer:
        `- [ ] {category}: {summary} — {file:line or area} (captured during {NNN-feature})`
        Categories: security, leak (memory/resource), convention, bug, perf, other.
        Security issues and leaks are the highest-priority captures.

     3. Audit finding written by /ductus — stricter form (see
        specs/008-security-rules/spec.md): `- [ ] {Rule ID}: {artifact} does not address — {summary}`.

     When an item is migrated, remove it from this list. -->

- [ ] convention: second promotion round — audit `AGENTS.md` against `framework/constitution.md` and move everything adopter-beneficial into the constitution, consolidating anything stated in both to the constitution with an `AGENTS.md` link. **Operator request 2026-09-13**, stated as two halves: (a) everything currently in `AGENTS.md` that can benefit adopters moves to `framework/constitution.md`, which is the one artifact adopters receive; (b) any instruction living in *both* is consolidated into the constitution, with `AGENTS.md` keeping a link. **Measured 2026-09-13.** `AGENTS.md` is 148 lines / 135,952 bytes carrying 103 top-level bullets: 92 rule-bearing across the four sections 050's plan scopes (Workflow 44, Gotchas 40, Boundaries 3, Design Principles 5) plus 11 descriptive (Project Structure 6, Tech Stack 5) that 050 correctly held out of scope. **41 entries cite a `framework/constitution.md#` anchor; 62 cite none.** Of the 41, only 31 use the established mirror phrasing (26 "contributor-side mirror", 5 "contributor-side record"), so roughly 10 cite the constitution without declaring themselves mirrors — which is exactly where half (b)'s duplication hides, since a restatement that happens to link is indistinguishable from a pointer by grep. The constitution is 760 lines across 32 anchored sections. **This is 050's territory and needs no new method.** §drift-prevention's canonical-sources map names `specs/050-constitution/spec.md` as the home for constitution-content work — "what belongs in it, how it is organized, which rules adopters receive" — and 050 already supplies every instrument this needs: the **reword test** (promote iff the rule restates without naming machinery only this repo has *and* without losing what makes it actionable), the three-tier **Classification** (universal → constitution; shared → an org's registered `[constitutions.*]` document per 055; project-only → stays), and **§Promotion mechanism** (one normative statement in the constitution, one `AGENTS.md` line that points and states nothing of its own — which is half (b) exactly). 050's plan already anticipates this run: "the table is a snapshot of one pass, and a later promotion round re-derives rather than amends it." **Root cause — do the flow fix first, before the 92-entry pass.** The backlog is not neglect and 050's AC1 did not cause it: AC1 correctly rejected a per-entry marker, since a field an author must remember to fill is the diligence dependency §design-principles rejects. The cause is that **every write-time routing rule discriminates by *kind*, while promotion discriminates by *population*.** Three sites tell an author where a learning goes and none asks the universality question: `AGENTS.md:5`'s header says only "append durable learnings to the matching section"; `CLAUDE.md`'s Auto-Memory Routing asks "would this help any other contributor **to this project**?"; and the constitution's §drift-prevention *Shared knowledge stays in git* answers by kind — *"A **project learning** — a convention, a gotcha, a workflow rule, a boundary — goes in `AGENTS.md`."* So a **universal** gotcha lands in `AGENTS.md` **because it is a gotcha**, not because anyone judged it project-only, and the population test that would catch it (050 §Classification: "one question about the rule's population") lives in a spec nobody reads while writing one. The default manufactures the backlog continuously — 54 rule-bearing entries at the survey, **92** now — so running the 92-entry pass without changing the routing just resets a counter that refills. **The fix is not a new authored input**, which is why §design-principles does not bar it: an author already chooses a destination, and changing *which question decides it* adds no remembered step. Move 050's population question into §drift-prevention's *Shared knowledge stays in git* bullets, and mirror it at `AGENTS.md:5` and in `CLAUDE.md`'s routing block — three small edits plus one 050 back-edge, landing **before** the bulk pass. **Correction to the counts above, which are proxies rather than measures.** Neither grep is the answer: the 62 non-citing entries **overcount** the backlog because 28 are settled project-only from 050's pass, and the 41 citing entries **overcount** the promoted set because project-only entries cite `§spec-lifecycle` and friends for context — 41 against the 26 that pass actually produced. A sound coverage check needs 050's classification table as an input, **re-keyed from positional IDs (`W4`, `G11`) to each entry's bolded lead phrase**, since positional IDs drift the moment an entry is inserted and the table is already stale by that mechanism alone. Then *unclassified = rule-bearing − (table-keyed ∪ constitution-citing)*, a new entry is unclassified **by default** (the honest state, and the opposite of a field that silently stays empty), and the number today is **~34**, not 62. Render it as a standing notice on the §brownfield-inbox inbox-row precedent — **notice, never gate**, because promotion has to stay free or the honest choice between a growing backlog and a silent one pushes toward silence. The re-keying is work this round must do anyway. **Honest limit on both halves:** neither makes promotion happen; one stops new misrouting and the other makes not-having-promoted visible. The reword test stays human judgment, which §runtime-boundary principle 2 keeps off the runtime regardless. **Scope note for whoever takes it:** this is a spec cycle of its own, not a session tail — 92 entries judged individually, each promotion reworded for adopter-neutrality with its reason recorded (AC1's audit trail), and every promoted entry's `AGENTS.md` line rewritten to a pointer. Expect the constitution to grow materially, so §Resolved Questions' bullets-not-sections rule and AC10's anchor-stability constraint both bind. Two constraints already settled and not to be re-litigated: a constitution-only change moves no version pin and needs no tag (050 §Resolved Questions), and the mirror stays in `AGENTS.md` rather than being deleted, because contributors read it as the index of how to work here and a rule that silently left reads as a rule that was dropped. **Routed 2026-09-14: a 050 back-edge, in two units and in the order this item already specifies — the root-cause flow fix first, then the bulk pass as its own spec cycle rather than a session tail.** **Every count above is superseded; re-measured the same day, and the decay is this item's own thesis borne out at the resolution of hours.** `AGENTS.md` is now **164 lines / 204,757 bytes** carrying **117** top-level bullets — **106 rule-bearing** across the four in-scope sections (Workflow 48, Gotchas 48, Boundaries 3, Design Principles 7) plus the same 11 descriptive ones 050 held out of scope — with **47** citing a `framework/constitution.md#` anchor and **32** using the declared-mirror phrasing. Against the 148 lines / 135,952 bytes / 103 bullets / **92** rule-bearing / 41 citing / 31 mirrors recorded above, the backlog grew **92 → 106 in a single day** and the file grew ~69KB. That is the item's own root-cause claim demonstrated rather than argued: the write-time default routes by *kind* and refills the queue faster than any bulk pass can drain it, so **running the bulk pass without the flow fix first resets a counter that refills**, and the ordering is not a preference. The corollary for whoever takes it: **do not promote entries opportunistically** while doing other work — a partial pass leaves the unclassified set unmeasurable, which is the state the standing-notice half exists to end. Re-derive the counts again at the start of the pass; the ones in this paragraph are evidence about 2026-09-14 and nothing more. **Re-derived 2026-09-15 and the queue grew again: 115 rule-bearing bullets (Workflow 49, Gotchas 55, Boundaries 3, Design Principles 8) across 177 lines / 220,627 bytes, with 49 citing a `framework/constitution.md#` anchor and 32 using the declared-mirror phrasing.** Against the 106 / 47 / 32 recorded above that is **+9 in a day**, and the split is the useful half: only **five** came from the `/ductus:init` retirement pass, so the other four landed from ordinary work in between. Nothing was promoted in that window. This is the item's own root-cause claim measured a second time — 92 → 106 → 115 while the promoted set moved by zero — and it is the argument for doing the flow fix first rather than a reason to hurry the bulk pass. Count with the same method before quoting these: top-level list bullets tallied per level-two heading, in-scope sections only. **2026-09-15: +1 from spec 056's pass — 116 rule-bearing, not 115.** One Gotchas entry was added (a recorded measurement can encode a constraint you have not found yet). It is flagged here rather than left for the counter to discover because it is a **universal-population** entry and therefore a promotion candidate on this item's own reword test: it was filed in `AGENTS.md` per the current write-time routing, not promoted opportunistically, which is exactly the default this item identifies as the root cause. Whoever runs the bulk pass should expect it in the unclassified set **2026-09-15, later the same day: +1 from the unresolved-`§`-reference pass — 117 rule-bearing, not 116.** Section split at that count: Workflow 49, Gotchas 57, Boundaries 3, Design Principles 8, across 178 lines. One Gotchas entry was **added** (`unexamined` cannot be measured while a spec is `in-progress`) and one existing Gotchas entry was **extended** rather than duplicated, which is why the counter moves by one and not two — worth stating because a pass that extends an entry and a pass that adds one are indistinguishable from the count alone. The new entry is **universal-population** on this item's own reword test: every adopter runs `/{project}:analyze` against the same `done`-only family scoping and the same pre-`done` gate, and the rule survives dropping every repo-only name. It was filed in `AGENTS.md` per the current write-time routing rather than promoted, which is the third consecutive demonstration of this item's root-cause claim — 92 → 106 → 115 → 116 → 117 while the promoted set has moved by zero. **2026-09-15, the adopter-name pass: +2 — 119 rule-bearing** (Workflow 49, Gotchas 59, Boundaries 3, Design Principles 8) across 180 lines, with 51 citing a `framework/constitution.md#` anchor. Two Gotchas entries were added and one existing entry was corrected in place; nothing was promoted, so the sequence is now **92 → 106 → 115 → 116 → 117 → 119 against a promoted set that has still not moved**. One of the two is a **universal-population** candidate on this item's own reword test and should be expected in the unclassified set: *`write-review` derives `scope` itself, so a count typed into the Summary can contradict the record it is written into* — every adopter runs the same primitive, and the rule survives dropping every repo-only name. The other (this repository's `grep` being ugrep, which errors per-file on a bounded-context pattern) is project-only by the same test. Both were filed in `AGENTS.md` per the current write-time routing rather than promoted, which is the discipline this item asks for — the flow fix landed earlier the same day stops *new* misrouting by making the population question the one an author meets, and it does not retroactively promote anything. **2026-09-15: the flow-fix half is DONE; the bulk pass is what remains of this item.** Landed through a `050` back-edge as task 19 and scenario `knowledge-routes-by-population-not-by-kind`: [§drift-prevention](../framework/constitution.md#drift-prevention)'s *Shared knowledge stays in git* now states two questions in order — *would this help a teammate?* then *who is it true for?* — and routes a project learning by the three §Classification tiers instead of sending every convention, gotcha, workflow rule and boundary to `AGENTS.md` because of its kind. **This item's site enumeration was an under-count and the missed half was the shipped half.** It named three sites; there are **five**. `AGENTS.md:5` and `framework/templates/project/agents.md:5` were **byte-identical**, and `CLAUDE.md` §Auto-Memory Routing has a twin in `framework/templates/project/claude-md.md` — so both adopter-facing templates seeded the by-kind routing into every new adoption, which is the mechanism rather than the instance. Fixing only this repository's three copies would have left it propagating. That is this item's own *re-derive what it touches, not only its counts* warning borne out, and it decayed toward under-estimating exactly as `AGENTS.md` §Workflow predicts. **Counts re-derived at the start of the pass and they held exactly: 117 rule-bearing (Workflow 49, Gotchas 57, Boundaries 3, Design Principles 8) across 178 lines**, so the +1-per-pass sequence above is unbroken and accurate. **This pass did not move it** — nothing was promoted and no `AGENTS.md` entry was added, deliberately, because promoting opportunistically while fixing the routing leaves the unclassified set unmeasurable, which is the state the standing-notice half exists to end. One small correction rode along on the line being rewritten: `AGENTS.md:5` named `Code Style` and `Testing`, which this repository does not have, and omitted `Design Principles`, which it does — inherited verbatim from the template, where all five names are correct. The two lines now differ deliberately. **What remains, unchanged in scope:** (a) the 117-entry promotion pass, its own spec cycle rather than a session tail, with each promotion reworded for adopter-neutrality and its `AGENTS.md` line rewritten to a pointer; and (b) the standing-notice half — re-keying 050's classification table from positional IDs to each entry's bolded lead phrase, then rendering *unclassified = rule-bearing − (table-keyed ∪ constitution-citing)* as a notice and never a gate. Neither is discharged by the flow fix, and the flow fix does not make promotion happen — it stops new misrouting, which is why it went first. **2026-09-15: half (b)'s re-keying is DONE and the coverage number is measured for the first time; only the *rendering* of it remains.** Landed as 050 task 20 (`analyze-stale`, priced by `check-review-gate` rather than judged — `plan.md` is not a durable contract, so it cost a reopen plus an analyze and no re-review). **All 54 §Classification rows are re-keyed** from the positional ID to the entry's `AGENTS.md` lead phrase, with the positional ID kept in parentheses so the section's own prose references to `W14`, `W15`, `DP1` and `DP2` still resolve. **The measurement, which is what the re-keying was for:** *unclassified = rule-bearing − (table-keyed ∪ constitution-citing)* = **119 − 75 = 44** (Workflow 7, Gotchas 34, Design Principles 3), against the **~34** this item estimated when the corpus was smaller. Two figures had to be reconciled before that number could be trusted, and **neither was decay**. **(1)** Re-deriving `AGENTS.md` at the classification pass's own commit (`9814d3ef^`) reproduced **54 rule-bearing entries exactly** — 34/16/2/2 — matching the table's recorded total, which is what validates the whole mapping. **(2)** The citing side reads **47**, not the **51** recorded above: 51 counts anchor-bearing *lines* in the file, including two outside the four in-scope sections, while the coverage formula needs *rule-bearing bullets in those sections*. A units disagreement of the same shape as the 19-vs-20 test-binary case, so **51 stays correct about lines** and must not be "fixed" — but do not feed it to the formula. **The key's own limit is stated in the section rather than left to be rediscovered**: a positional ID degrades *silently* to matching the wrong entry once anything is inserted above it, while a lead phrase degrades *loudly* to not matching at all when an entry is reworded — which five of the 54 had already done and which were re-matched by hand, the other 49 matching mechanically. That is why the lead phrase is the better key and still not a perfect one. **What remains of half (b) is the rendering, and it is a release rather than a doc edit.** The notice is to follow the §brownfield-inbox inbox-row precedent, which is rendered from a runtime primitive's result field — so it is `runtime/` work carrying a version bump and a `ductus-v<version>` tag, not a session tail. **Half (a) — the 119-entry promotion pass — is untouched and still its own spec cycle.** Nothing was promoted in this pass, deliberately: a partial pass leaves the unclassified set unmeasurable, which is the state this half exists to end. What changed is that the set is now *measurable from `AGENTS.md` on disk* rather than from git archaeology, so whoever runs half (a) can re-derive 44 in one command instead of reconstructing a four-week-old file. **2026-09-15: half (a)'s classification is DONE — the 44 are judged and the unclassified count is discharged to zero. What remains of half (a) is the promotion itself.** Landed as 050 task 21, the same shape as that spec's own task 1 (*classify all 54 rule-bearing entries*), which is the precedent for classification being its own task with promotion following separately — so this is not a partial pass, it is the first of the two the first round also took. **Verdicts: 26 to promote, 1 routes to a rule file rather than the constitution, 1 already promoted in substance, 16 project-only.** The rule-file case is worth carrying because it is a *fourth* outcome this item never anticipated: *replacing a hand-rolled predicate with a shared one widens what it quietly did* is universal by the reword test but governs **code** rather than the pipeline, and [§rules](../framework/constitution.md#rules) puts a cross-cutting code concern in a `quality-cross` rule with an ID, not in the constitution. A three-tier classification cannot express it, so it is recorded as its own table. The already-promoted case is the first round's W14/W15 shape repeating: [§numbering](../framework/constitution.md#numbering-convention) already states that the feature-directory membership rule is defined in exactly one place and called rather than restated, so what remains in `AGENTS.md` is only the Rust function name. **Second-round rows are keyed by full lead phrase like the first**, and each key was asserted a **1:1** match against its entry with a minimum margin of 0.33 rather than taken on a best guess — the first attempt keyed them by paraphrase, which would have recreated the very matchability problem the re-keying existed to solve. **Coverage verified rather than asserted:** the 98 keys both rounds carry resolve to 98 distinct entries, and 98 ∪ 47 constitution-citing = **119**, the whole rule-bearing set, so *unclassified* is **0**. **What remains of half (a) is the 26 promotions** — each reworded for adopter-neutrality with its reason already recorded in the table, each `AGENTS.md` line rewritten to a pointer, plus the one rule-file entry routed to `quality-cross`. Expect the constitution to grow materially, so §Resolved Questions' bullets-not-sections rule and AC10's anchor-stability constraint both bind. That is the unit this item has always called a spec cycle; the difference now is that its worklist is enumerated with reasons rather than estimated. **The worklist is `specs/050-constitution/plan.md` §Classification, second-round tables** — `#### Second round — promote` holds the 26 with a reason each, `#### Second round — routes to a rule file, not here` holds the one bound for `framework/rules/quality-cross.md`, and the first round's three tables above them are already discharged. Rows in both rounds are keyed by the entry's `AGENTS.md` lead phrase, so each one greps straight to its entry. **Re-derive the counts before starting it anyway** — this pass measured 119 rule-bearing, and every prior pass in this item's history measured a larger number than the one before. **And re-derive the coverage the same way the section documents it**: *unclassified = rule-bearing − (table-keyed ∪ constitution-citing)*, counting the citing side as rule-bearing bullets in the four in-scope sections (**47**), never as anchor-bearing lines in the file (**51**) — the two differ and only the first belongs in the formula.
- [ ] bug: 050's retired-filename sweep (`510eb25c`) inverted at least two legacy-describing records, and 9 further candidates are unread. **The shape**: the sweep substituted the current name into a sentence whose subject is the *retired* state, so the sentence still parses and asserts the opposite of what it recorded. `AGENTS.md` §Gotchas prescribes grepping a sweep diff for `X → X`; that cannot see this, because the two sides genuinely differ. **Two confirmed and fixed 2026-09-14**, both during 048's backfill pass: `specs/048-govern-acquired-runtime/scenarios/retired-namespace-tools-are-off-limits.md` read *a pre-`.ductus/` binary resolves `.ductus/`* (self-contradictory; the shipped `framework/bootstrap/ductus.md` still read `.govern/` and was correct throughout), and `specs/026-framework-self-audit/scenarios/family-23-sweep-target-manifest-parity.md` read *`.ductus/scripts/lib/specs-root.sh` kept resolving `.ductus/config.toml` then the legacy root with no `.ductus/` tier* — inverting the incident the scenario exists to record, which `AGENTS.md` carries correctly. **How the second was found is the durable half**: fixing the first removed one substitution from the sweep, which costs `mechanical_sweep`'s **repo-wide** uniformity test, and Family 19 immediately reported 026's review stale — a true signal the exemption had been masking. Proven by probe (Family 19 exits 0 at `9188d865`, 1 at `8d660cb3` in a detached worktree). Restoring 026's line returned it to `9dfa1299`, exactly its recorded `reviewed-digest`, so the gate went green with no review re-run and no claimed pass; both repairs are mechanical under [§spec-lifecycle](../framework/constitution.md#spec-lifecycle)'s *sweep residue where a substitution landed in a sentence it did not fit*, so neither spec left `done` for it. **Measured, not estimated**: `510eb25c` made 319 substitutions across 60 files; an aligned per-file diff filtered to pairs that removed the *last* retired token from a sentence carrying a legacy marker (`kept resolving`, `used to`, `pre-0N`, `legacy root`, `historical root`, `no longer exists`, `was the pre-`) yields **11 candidates across 8 files** — the two above plus 9 unread in `specs/005-workflows/spec.md` (now consolidated into 043), `specs/017-derive-dont-ask/{spec.md,data-model.md}`, `specs/022-deterministic-runtime/data-model.md` (its *historical roots* bullet is the strongest remaining candidate), `specs/027-bootstrap-migration-registry/spec.md` (three, where the dropped text is a `**superseded naming**` annotation rather than an inversion — possibly 050 acting deliberately), and `specs/029-bootstrap-runtime-autowire/scenarios/project-inputs-asked-once.md`. Each needs reading in full context; the filter is a candidate generator, not a verdict. **And the hunt is scoped too narrowly: 049's `9da4a7ae` produced the same shape and has never been run through the generator.** Found 2026-09-14 in 026's pass — `scenarios/host-namespace-parity.md` records a mismatch between the resolved namespace and the installed one, and 049 mapped **both** `govern` and `gov` to `ductus`, collapsing the two sides onto one name so the sentence asserted that `/ductus:*` names a namespace that does not exist while `.claude/commands/ductus/` sat beside it. Repaired in that pass. The generalisation, worth more than the instance: **a many-to-one substitution table is the tell** — a sweep that sends two distinct old tokens to one new token can flatten any sentence whose point is that the two differed, and the `X → X` grep cannot see it because neither side is unchanged. Re-run the generator over `9da4a7ae` as well as `510eb25c` before calling this item measured. **Cost by destination, which is what sets this**: every remaining candidate sits in a spec the corpus has already closed, so the two-disposal rule in `AGENTS.md` §Workflow binds — and four of the eight files are durable contracts (`data-model.md` / `scenarios/*.md`), each costing a reopen **plus a full five-pass re-review**, with 022 the corpus's largest. The `spec.md` hits reopen their spec but stale no review. Route with the other 022-blocked items rather than singly. **Not a mechanical sweep to run blind**: the correct repair differs per line, and the wrong one destroys the record a second time. **Routed 2026-09-14, and the unread count is 8, not 9** — one of the named candidate files, `specs/005-workflows/spec.md`, no longer exists, because 005 was consolidated into 043 on 2026-09-13; verified absent rather than assumed. **Disposal per destination, under the two-disposal rule in `AGENTS.md` §Workflow**: the strongest remaining candidate was `022/data-model.md`'s *historical roots* bullet and it is now **discharged** — read in the 2026-09-14 batch reopen and **confirmed as a real inversion**, bringing the confirmed count to four. It read `scripts/` → `.ductus/scripts/` → `.ductus/scripts/`; the pre-sweep bytes show the middle stage was `.govern/scripts/`, and the sentence's whole subject is that the chain had multiple roots. Unlike the first two, this one **is** visible to the prescribed `X → X` grep, which is worth knowing: the class has both shapes, so run the grep *and* the generator. That leaves **7 unread**; `017/spec.md` and `027/spec.md` (three) reopen their spec but stale **no** review, so each is a reopen plus an analyze; and `017/data-model.md` plus `029/scenarios/project-inputs-asked-once.md` are durable contracts costing a reopen **plus a full five-pass re-review** each, which is what makes them worth sequencing against any other reason to open those two specs. **The half most likely to be lost, so it is stated last and plainly: the generator has still never been run over `9da4a7ae`.** 049's sweep produced a confirmed instance of this exact shape and is a **many-to-one** substitution table (`govern` and `gov` both to `ductus`), which is the tell — a sweep that sends two distinct old tokens to one new token can flatten any sentence whose point is that the two differed, and neither the `X → X` grep nor this item's own filter sees it. Run the generator over `9da4a7ae` as well as `510eb25c` before calling this item measured (captured during /ductus:review of 048-govern-acquired-runtime) **Sized 2026-09-15 so the unrun half can be priced: `9da4a7ae` is 389 files changed / 4,110 insertions / 4,003 deletions, against `510eb25c`'s 70 files / 319 substitutions.** An order of magnitude wider, so the generator run over it is its own unit of work rather than a tail on the seven unread candidates — and it is the half with a confirmed instance already found by hand (`026/scenarios/host-namespace-parity.md`), so it is not speculative. **2026-09-15: both `017` candidates are read and judged NOT damage — the unread set is smaller, and this item's own arithmetic does not add up.** Discharged on the way past, inside the `017` reopen the adopter-name sweep already required, so they cost no extra reopen and no extra re-review. `017/spec.md` read *Pinning is `.ductus/config.toml` `[pinned] files` — `.govern.toml` was the pre-049 name and no longer exists*, and `017/data-model.md` read the same shape one clause shorter; in both, `510eb25c` removed the **trailing decision-record clause** rather than inverting the sentence, leaving a statement that is true, non-contradictory and correct about current behaviour. The generator flagged them because the removed text carried a legacy marker (`no longer exists`, `pre-049`), which is the filter working as a candidate generator rather than a verdict — exactly as this item warns. What is lost is one historical note per file, and `049`'s own spec plus git history hold that record, so no repair is warranted. **The arithmetic**: this item's remaining enumeration names `017/spec.md`, `017/data-model.md`, `027/spec.md` (three) and `029/scenarios/project-inputs-asked-once.md` — **six**, not the seven it claims, and the same off-by-one is visible one sentence earlier where eight is derived from a list of seven. Do not act on either count; re-derive from the generator. With the two `017` files discharged, the named-and-unread set is `027/spec.md` (three, reopens 027 and stales no review) and `029/scenarios/project-inputs-asked-once.md` (a durable contract, so a reopen plus a full five-pass re-review). **The `9da4a7ae` half is untouched and remains the larger unit**, unchanged in scope: 389 files / 4,110 insertions against `510eb25c`'s 70 / 319, with one confirmed instance already found by hand. **2026-09-15: the `9da4a7ae` half is RUN, and every candidate this item ever *named* is now read.** **The recorded generator definition is lossy and had to be corrected before it could run at all** — its marker list (`pre-0N`, `legacy root`, `historical root`, `no longer exists`) matches **neither** confirmed instance's actual wording: `510eb25c`'s sits on a `pre-` prefix in front of a backticked path, and `9da4a7ae`'s carries *does not exist*, the present-tense spelling of the negative-existence family. Widened to the families the recorded list names in narrowed form (`pre-`, `legacy`, `historical`, plus *does not exist* / *do not exist* / *never existed*) and calibrated on **recall over all three confirmed instances across both sweeps**, which is the acceptance test a candidate generator has to pass: the first run reported 277 candidates over a filter that provably could not see `026/scenarios/host-namespace-parity.md`, the one hit somebody had already found by hand. Re-running the corrected filter over `510eb25c` gives **33 candidates / 22 files** against the recorded **11 / 8** — a **method** difference and not decay, the recorded instrument being the narrower one, so do not correct 11 / 8 as wrong. **A second filter, built from this item's own *many-to-one is the tell* observation, which had never been turned into an instrument**: two or more distinct retired spellings in the pre-sweep sentence and zero after. Over `9da4a7ae` it yields **144 candidates / 60 files**; over `510eb25c` it yields **0**, which is the confirmation that it is specific to the shape, since `510eb25c` is a single-token filename sweep with no many-to-one table to flatten anything. The two filters are **not nested** — the flattening one surfaces durable contracts the prescribed one misses entirely (022's `govern-bootstrap.md`, `commands-dir-parameterization.md`, `opencode-command-resolution.md` and `runtime-review-019-hardening.md`; 019's and 028's `data-model.md`; 029's two scenarios; 033's `degenerate-surfaces-config.md`). Run both. **`9da4a7ae` measured: 402 candidates / 123 tracked files** (union of both filters; 9 further candidates sit in 4 files since deleted and are not work). By destination, which is what prices it: **durable contracts 46 / 28 files** (reopen plus a FULL re-review), **`spec.md` and `plan.md` 238 / 49** (reopen plus an analyze), **`tasks.md` and `review.md` 69 / 26** (ephemeral and regenerated — no repair), **non-spec 49 / 20** (free). **The expensive tier is READ IN FULL this pass and is clean.** All 46 durable-contract candidates across 28 files were read in context. Exactly one is a genuine inversion — `026/scenarios/host-namespace-parity.md`, already repaired 2026-09-14, whose line now reads past-tense and keeps both retired names deliberately because the mismatch **is** the record. The other 45 are the rename working rather than damage: 049 unified **three surfaces of one project** — the `gov` command namespace, the `govern` project and bootstrap name, the `gvrn` binary and crate — so a sentence naming two of them and collapsing both onto `ductus` is correct, which is why a bare many-to-one count over-reports so heavily here. **So `9da4a7ae` owes no reopen-plus-full-re-review**, which was this item's largest outstanding cost. **Tier B was narrowed by the shape that produced the one real hit, and the narrowing is validated rather than assumed.** The damaging sentence's *subject* is a mismatch, so filtering the union to a contrast marker (*mismatch*, *does not exist*, *differ*, *whereas*, *instead of*, *rather than*, *while the*, *but the*, *out of sync*) alongside two or more distinct retired spellings should isolate damage. Run against tier A, where this pass established the ground truth, it takes **46 to 2**, and those 2 are **exactly** the two damaged lines of the one real inversion — no false negative and no false positive on the only tier that can check it. Applied to tier B it takes **238 to 2** (`042/spec.md:90` and `048/spec.md:146`), both read and both correct. **The residual, stated rather than implied**: the other 236 are excluded by a validated filter, not read line by line, which is a weaker claim than tier A's and is the one place this measurement rests on an instrument rather than on a reading. **The named-and-unread `510eb25c` set is discharged.** `027/spec.md`'s three are **050 acting deliberately**, as this item suspected: the sweep *rewrote the annotation paragraph* rather than substituting into it, replacing *they say the retired filename, which no longer exists* with a statement that the criteria now name `.ductus/config.toml` and why sweeping them was right, citing [§drift-prevention](../framework/constitution.md#drift-prevention)'s own exemption and keeping the load-bearing half (*a migration must name the file it migrates from*). `029/scenarios/project-inputs-asked-once.md`'s three are the same not-damage class as the two `017` files — the claim (the name was already persisted and the procedure never read it back) survives the substitution and is true of the current file. **A fifth confirmed instance, found by the corrected filter and never named before — priced rather than repaired.** `specs/022-deterministic-runtime/scenarios/write-session-primitive.md` carries a paragraph explicitly labelled **History note** recording the sequence of session-file path choices, and `510eb25c` rewrote its 0.10.0 destination, so it now reads *the 0.10.0 consolidation moves the file to* `.ductus/session.toml` when 0.10.0 moved it to the then-current root filename — the `.ductus/` path came later, from 042 and 049/050. That is `AGENTS.md` §Gotchas' *a sweep must not rewrite the record of the thing being swept* landing in a history note, and the paragraph keeping the older `gov-session.json` spelling untouched beside it is the tell. Four of that file's five substitutions are correct; only the history note is damage. **Why it is not repaired here, measured by probe in both directions rather than reasoned:** 022's review is current against `23a5fd80`, which is *after* the sweep, so its `reviewed-digest` covers the damaged bytes and the cheap restore-the-digest disposition does not apply (the sweep changed five lines in that file, not one). `scripts/audit/review-freshness.sh` exits **0** on the tree as it stands and **1** with the one-line repair applied, naming that file and 022 — a hard release gate — so repairing it obliges a **full five-pass re-review of 022**, the corpus's largest single unit. Route it with 022's next deliberate reopen rather than singly; the repair itself is one line and is **mechanical** under [§spec-lifecycle](../framework/constitution.md#spec-lifecycle), so it reopens nothing on its own. **What remains of this item**, all of it cheaper than what closed: tier B's 236 excluded-by-filter candidates and the ~22 additional `510eb25c` candidates the corrected filter surfaces beyond the recorded 11, neither read line by line; plus the 022 history-note repair above, waiting on a 022 reopen. Nothing remaining is a durable contract except that one line. **2026-09-15, same day: the reading half is DONE — every candidate from both sweeps is now read except the two tiers that are not worth repairing, and six defect classes were fixed in the pass.** Read in full this pass: `9da4a7ae` tier D (49 candidates / 20 files, non-spec) and tier B (216 live / 49 files), plus `510eb25c`'s 24 live non-durable candidates. Not read, deliberately: the `tasks.md` / `review.md` tier (69), which is ephemeral and regenerated — a reference fixed in `review.md` is destroyed by the next `write-review`. **A methodological correction that changes how any of this is counted: a generator over an old sweep reports what the sweep *did*, not what the tree *carries*.** `9da4a7ae` is a month old, so its candidates have had a month of subsequent edits landing on them: of 402, **343 are still present in the working tree and 59 were superseded** by later work and are not work now. The tier-D flattening that would otherwise have been the headline — `README.md`'s *The `govern` runtime (`gvrn`)* collapsing to *The `ductus` runtime (`ductus`)*, a parenthetical restating its own subject — is in that superseded set, already repaired, as is the neighbouring `PATH` claim that 048 falsified. **Check each candidate against the current tree before pricing it**; the diff alone over-reports. **Six classes fixed in this pass, all mechanical and none reopening a spec** ([§spec-lifecycle](../framework/constitution.md#spec-lifecycle)'s *sweep residue where a substitution landed in a sentence it did not fit*), and all outside the durable-contract tier, so `scripts/audit/review-freshness.sh` exits **0** both before and after — verified rather than assumed. **(a) `/gov:govern` collapsed to `/ductus:ductus`**, a command that does not and never did exist: the *namespaced* bootstrap invocation carried two distinct retired tokens (`gov` and `govern`) in one string, and the many-to-one table sent both to `ductus`. Three occurrences, in `010/plan.md` and `010/tasks.md`; repaired to `/ductus`, which is what every sibling occurrence reads. **(b) `023/plan.md`'s release pair flattened.** That plan's whole subject is **two** artifacts released separately — the `gvrn` binary and the `govern` framework — and collapsing both onto `ductus` left four sentences that say nothing or contradict themselves: *Two clean releases (`ductus` first, `ductus` second)*, *the ductus / ductus release pair*, *Ship `ductus` and `ductus` changes in one combined release*, and *the dependency points one way (`ductus` needs the new `ductus` primitives)* — the last asserting that a thing depends on itself. Repaired by naming the two by role (runtime binary / framework) rather than by either name. **(c) A new defect class with an objective tell, and it is the generalisable half: a recorded migration whose *source* was left in its historical form while its *destination* was swept, so the sentence now narrates a move that did not happen.** The tell needs no judgement — within one sentence, one path is pre-rename and the other is post-rename, so the two halves disagree about which era they are in. Five sites, all from `510eb25c` and all confirmed against `510eb25c^`: `022/spec.md` (*pre-0.10.0 it was hardcoded to* `.claude/gov-session.json` … *the 0.10.0 consolidation routes the session through* the **current** path), `022/plan.md`, `023/spec.md` twice, and `022/scenarios/write-session-primitive.md`. Four were repaired here for free by naming what each release actually did and where the file lives now. **The fifth is the durable contract already recorded above and is still deferred** — it is the same class, so repair it in the same pass whenever 022 is next reopened. `007/spec.md`'s *post-0.10.0 it lives at the repo-root* reads as a present-tense claim that is true today and was deliberately left; `000/spec.md`'s similar line was **not touched by this sweep** (verified against the commit, not inferred) and is out of scope. **Running total: seven confirmed classes across the two sweeps, six repaired, one outstanding** — the `022` durable-contract line, whose price was measured by probe and is a full five-pass re-review of the corpus's largest spec. **What remains of this item is now one line of work plus one tier nobody should spend on**: the `022/scenarios/write-session-primitive.md` repair, waiting on a 022 reopen to ride along with; and the 69 `tasks.md` / `review.md` candidates, which are ephemeral or regenerated. Every other candidate in both sweeps has been read.
- [ ] convention: six `AGENTS.md` entries independently state one rule — **a recorded measurement is re-derivable only if it states its method and its units** — and not one of them states it as the rule, so each disagreement is met as a fresh puzzle. **Measured 2026-09-15**, the fragments and where they live: `G54` (a recorded measurement can encode a constraint you have not found yet), `G55` (when two greps in one session disagree, re-derive both), `G40` (*a measurement is a claim, and its scope is part of the claim*), `G23` and `G30` (a count is evidence about the day it was taken), `G39` (the 19-vs-20 test-binary case, recorded as *a units disagreement rather than decay*), and `W46` (an enumeration decays toward **under**-estimating). **Four instances in one session, each costing a re-derivation and none of them decay**: the sweep-inversion generator's recorded marker list, which provably could not match either confirmed instance it was written from; the promotion item's **51** constitution-citing entries (anchor-bearing *lines*) against the **47** the coverage formula needs (rule-bearing *bullets in the in-scope sections*); the 19-vs-20 binary count; and `027`'s `unexamined` overwritten from a measured 3 to a bare 0. **The synthesis is in none of the six, and it is the actionable half**: every one of those four resolved to a difference of *method or units*, never to the record having decayed — while the reflex on meeting a disagreement is to trust the newer figure, which would have been wrong all four times. **Load-bearing exclusions, so this is a gap rather than a duplicate**: [§grounding](../framework/constitution.md#grounding)'s *Cite what you consulted* requires naming the **source** and says nothing about method or units; [§design-principles](../framework/constitution.md#design-principles)' check-that-cannot-run covers a check that did not run, not a measurement that ran with an unstated denominator. **Population: universal** on 050's reword test — every adopter records `examined` / `scope` / `unexamined` and writes counts into review and analyze Summaries, and the rule survives dropping every repo-only name — so it routes to the constitution through a `050` back-edge. **It is a *new* rule, not a promotion**: do not fold it into half (a)'s 26-promotion count. **And do not discharge it by adding a seventh `AGENTS.md` entry** — six scattered statements of one rule is the condition this item exists to end, and a seventh would be that condition committed again.
