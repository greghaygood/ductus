# Inbox

<!-- Rules:
     - Do not frontfill bugs that are not being actively worked on.
     - A bug or omission inside the scope of the spec currently in progress does NOT belong
       here — it becomes a task on that spec's tasks.md. The inbox is for findings with no
       home yet; an in-progress spec is already the home, so an item logged here is routed
       straight back to it (constitution §brownfield-inbox, scope decides the destination).
     - Write specs for areas being actively touched — let adoption spread naturally.
     - As specs are written, items migrate from here into spec updates or new scenarios.
     - Chores (project maintenance with no feature home — lint/formatting cleanup,
       dependency cleanup, repo hygiene) also live here; /groom recognizes them and leaves
       them in place. They clear when done, not by migrating to a spec.
     - The brownfield backlog drains toward empty as adoption completes; incidental
       capture is ongoing, so the file persists while work keeps surfacing issues.
     - Status notes do NOT belong here. Every item must be routable by /groom to one of
       its five routes (rule, spec, scenario, chore, discard); a "where things stand"
       or "what to do next" note matches none of them, so it would be walked and
       re-discarded on every pass forever. Pipeline state is derived — read it from
       /status, tasks.md, and git — not narrated into the backlog.

     Format each item as a checkbox list entry with a brief description and any relevant
     context. Three forms are in use:

     1. Manual entry (via /log) — the simple form below:
        `- [ ] {Brief description of the issue and any relevant context}`

     2. Auto-captured finding (an agent recorded this automatically while working a task,
        per §brownfield-inbox Automatic issue capture). Lead with a category so /groom can
        route it, and include a source pointer:
        `- [ ] {category}: {summary} — {file:line or area} (captured during {NNN-feature})`
        Categories: security, leak (memory/resource), convention, bug, perf, other.
        Security issues and leaks are the highest-priority captures.

     3. Audit finding written by /ductus — stricter form (see
        specs/008-security-rules/spec.md): `- [ ] {Rule ID}: {artifact} does not address — {summary}`.

     When an item is migrated, remove it from this list. -->

- [ ] convention: backfill `examined`/`scope` and the freshness digests on the spec records that predate `ductus-v0.49.0`. **Why:** measured 2026-09-13, 49 of 55 review records carried no `examined`, so a `0/0/0` review is byte-identical to one whose five passes never fired; 46 carried no `reviewed-digest` and 45 no `analyzed-digest`, so the pre-`done` gate reports freshness as *undeterminable*, which does not block. Re-running `/ductus:review` then `/ductus:analyze` per spec is what writes the fields, and it is only honest if the passes actually read the resolved scope — see §Workflow's review-is-the-five-passes entry and the 017/048 incident it records. **Done (45):** 001, 002, 003, 004, 006, 007, 008, 009, 010, 011, 012, 014, 015, 016, 017, 018, 019, 020, 021, 022, 024, 025, 027, 028, 029, 030, 031, 032, 033, 034, 035, 036, 037, 038, 039, 042, 043, 044, 046, 049, 050, 052, 054, 055. **Remaining (9), in order** — every one carries scenarios or a data-model, so `reviewed-digest` is non-empty and freshness becomes judgeable: 045, 041, 040, 013, 051, 023, 000, 048, and **026 last** (22 scenarios, 1391 lines). **030 closed 2026-09-13 and is the corpus's sharpest worked example of step (5).** Its pre-reopen natural base `40c43175` resolved **810** modified-since / 814 in scope against a plan affecting 21 — the widest gap in the corpus — and the reopen collapsed it to **4 / 24** on base `527c052d`; `--since HEAD` gave 0 / 21 pre-reopen and 21 / 0 after, and was declined both times because it excludes the pass's own edits by construction. Recorded 0/0/0, examined 15 of 24, unexamined 6. The pass found AC10 asserting two clauses 048 reversed (`ductus` "never a prerequisite", and a no-runtime CI job deleted in `2cca7d6d`), and `data-model.md` plus the scenario still naming `scripts/gen-cross-service-refs.sh` as the live harvester after `f4c3bbd3` made it the `derive-references` primitive — 022's promotion sweep updated 017 and 018 but missed 030. **Its `unexamined` is the 028 lesson repeated**: `skipped` was `[]` at `in-progress` and **6** at `done`, and all six entries were introduced by the pass's own AC10 annotation, which gave that criterion its first backticked paths carrying an interior slash. **032 closed 2026-09-13**, and it is the corpus's worked example of the *fix-in-the-pass* stance rather than of step (5). Recorded 0/0/0, examined 9 of 11, unexamined 0. Step (5) went as predicted: the pre-reopen natural base `5fdd7390` (032's own planning commit) resolved **811** modified-since / 812 in scope against a plan affecting **9**, `--since HEAD` gave 0 / 9, and the reopen collapsed the natural base to **5 / 11** on `3c423743` — taken, because it covers the five files the pass edited, which HEAD excludes by construction. The natural-base call exceeds the MCP output cap, so it was run through the CLI and read with `jq`. **Seven defects, all fixed in the pass, none captured** — the inbox stayed at 10. Two were 032's own: 049's sweep had half-applied to the OpenCode namespacing example (its map carried `command/gov/` → `command/ductus/`, so it rewrote the path and left the bare key `gov/specify` beside it, six occurrences asserting that a `ductus/` subdirectory yields a `gov/` key), and `data-model.md` depicted the `bash` permission map with the broad `"*": "ask"` **last**, which under OpenCode's last-match-wins shadows every allow *and* downgrades the `rm -rf *` deny to an ask — the document contradicting its own note two lines below. Only that second one was a factual correction, so it is what took the back-edge; the sweep completion is mechanical under §spec-lifecycle. The other five are the **028 lesson landing again, and the reason this spec was flagged as judgment work**: five layout-derived values stated as `claude-style` universals in shipped installer prose — the pinned-`ductus.md` advisory enumerating two of three install paths, the security-audit summary rendering `/{project}:groom` with no per-agent instruction, three sites naming one command-resolution path where `host.rs` resolves two, and step 9 naming the wrong self-install directory for an OpenCode adopter. **What found them was finishing the SHAPE grep, not the row-by-row walk**: `/{project}:` and `{config_dir}/commands/` were clean after 028's pass, and `{cli-config-dir}/commands` plus `commands/ductus.md` were not — so grep every spelling of the shape, not the two the entry happens to name. `unexamined: 0` is a real zero and was verified by hand: `is_path_like` rejects any span carrying braces or angle brackets, so of five slash-bearing backticked spans in 032's criteria the family had exactly **one** candidate, and the other four were checked against the tree by hand. **008 closed 2026-09-13, and it is the corpus's worked example of a defect whose source is the spec's own canonical record.** Recorded 0/0/0, examined 7 of 21, unexamined 6. Step (5) as predicted: the pre-reopen natural base `043a0345` resolved **83** modified-since / 88 in scope against a plan affecting **6**; the reopen collapsed it to **20 / 20** on `e6f07be8`, and to 21 / 21 once the review commit put `review.md` in the window. `--since HEAD` gave **0 / 6** and was declined for excluding the pass's own edits. Both legs returned inline, so the base was chosen on the merits with no friction asymmetry. **Twelve defects, all fixed in the pass, none captured — the inbox stayed at 10.** The largest was a **202-occurrence prose-claim sweep across 13 live files, 11 of which ship to adopters**: `validate` was renamed to `/analyze` by 023, whose AC21 swept four token forms (`/validate`, `/ductus:validate`, `/{project}:validate`, `validate.md`) and left the bare prose name in every one. It traces to **008's own `data-model.md`** — the constitution's canonical-sources map designates it THE source for security rule file format and Verification phrasing, and it prescribed "instruction to the validate agent", which all eleven rule files copied into 187 Verification fields. `quality-cross.md` (036, the newest rule file) already carried the correct wording and is what the ten stale headers were aligned to; **the outlier is the drift**, as with 032. Fixing the canonical source and leaving the eleven files it seeded would have repeated the partial sweep that caused it, so all thirteen went in one commit — none is a spec directory, so nothing reopened and no other review went stale. **The one that was a real behavioural gap:** AC16 promised an advisory when no rule files are present, 016 kept and generalized it (`No rule files found, skipping rule checks`), and **022's task 14 (`11aad341`) deleted it during the parseable-procedure rewrite with no decision to drop it** — so a project with no rule files gets a `loading rule files:` line with nothing after the colon and no warning, which is examined-and-empty rendering as could-not-examine. Restored to `analyze.md` as a sweep to finish rather than editing the criterion to match the tree. **Two miscited rule IDs, both checked against git history rather than assumed:** §Verification phrasing cited `BE-API-002` as "TLS for production traffic", and `35502af0` shows it has been the `Server`/`X-Powered-By` suppression rule since the file was written — the citation never matched, so the example now quotes `BE-DATA-001`'s shipped Verification verbatim. And `FE-XSS-002` was labelled "output encoding strategy" (that is `FE-XSS-001`) in 008 **and in the shipped `framework/templates/spec/spec.md`**, which copies into every adopter. The rest: the pre-0.6.0 `specs/security-{backend,frontend}.md` destination at three sites, **invisible to `criterion-path-existence` because `is_path_like` rejects braces**; "Backend rules apply to all projects", falsified by 033's `[rules] surfaces` filter, with RQ3's whole-file-pinning premise superseded by the same spec; the `NNN-*` digit grammar restated in `ductus.md`'s Security Audit against §numbering and 051 — and then, **found at the analyze step after the status flip**, the same gloss still in 008's own AC22/AC23 and §Trigger, where AC23's "greenfield = no `specs/NNN-*` directories" went outright false for a branch-scoped-only corpus; `data-model.md`'s claim that category headings match the spec's list "verbatim" (13 of 15 differ, 4 substantively) plus its file-structure template showing `# {Surface} Security Rules` / `## {Category Name}` against shipped `# Security Rules — Backend` / `## BE-AUTHN — Authentication`; and in plan.md and research.md, GraphQL and software supply chain recorded as uncovered/deferred while both ship, "Governance-owned" (049), `CLAUDE.md` for `AGENTS.md`, 005 (consolidated into 043 the same day), the Password Storage cheat sheet mapped to BE-DATA when only `BE-AUTHN-001` cites it, and RFC 7807 where `BE-ERR-002` already says RFC 9457 obsoletes it. **Verified rather than assumed:** all 105 rule IDs carry Statement/Rationale/Verification, are unique, and are zero-padded from `001` per category with no gaps; the BE/FE category sets match `data-model.md`'s table and RQ2's enumeration exactly; the 28 criteria are all checked and the three unchecked boxes are §Brownfield Adoption's illustrative capture format, which Family 27 is correctly quiet on. **`unexamined: 6` is the same at `done` as it was before the pass** — the pass replaced brace-bearing paths with backticked ones carrying interior slashes, but they duplicated candidates already in the set, so the deduplicated array did not grow. Walked entry by entry anyway. **advisory 0 after excluding the documented false positive by hand:** `check-orphaned-references` reported `AGENTS.md -> specs/rules/` **twice** (lines 119 and 120, the `check-rule-ids` entry and the entry documenting this very false positive), both Shared Files manifest destinations. **031 closed 2026-09-13, and it is the corpus's worked example of a *non-goal* that was already false when it was written.** Recorded 0/0/0, examined 8 of 10, unexamined 2 (both `root-absent`, unchanged by the pass). Step (5) behaved as predicted: the pre-reopen natural base `0f8e334a` resolved **813** modified-since / 814 in scope against a plan affecting **8**, and the step-4 commit collapsed it to **3 / 10** on `dd8ce2a3`; `--since HEAD` gave 0 / 8 both times and was declined for excluding the pass's own edits. The natural-base leg was run through the CLI and read with `jq` as planned, but **post-reopen it came back at 1020 bytes** — the MCP cap is a property of the *pre-reopen* window, so the friction asymmetry the warning describes never materialised on the leg that actually decided anything. **Five defects, all fixed in the pass, none captured** — the inbox stayed at 12. The largest was a **non-goal refuted by its own dependency**: §Out of Scope deferred verifying whether Antigravity skills/settings load from project-local `.agents/`, claimed it was "tracked as a separate follow-up", and pointed at an "Open Question below" that is a **Resolved** Question. Nothing tracked it — no spec, no scenario, no inbox item — and `028-antigravity-agent` had **already run that verification and recorded it on 2026-06-09, eight days before 031 was created**: a live `agy` session loaded `<repo>/.agents/skills/probe-ws/SKILL.md` while `.claude/` probes did not, and `.agents/settings.json` is the workspace permissions file. So `.agents/` is read for skills and settings and ignored only for MCP. Retired rather than captured: the question the follow-up would have asked already has an answer. **The other four are the 032 lesson from the other side.** The surfaced Auggie command was stale in three places (AC7, the Auggie Resolved Question, `data-model.md`), all quoting `--command ductus` — a bare command `048` stopped resolving, with `framework/migrations/runtime-store-path.md` shipped specifically to repoint adopters off it. `data-model.md`'s "Server entry shape (unchanged across all agents)" went false when 032 added an agent using an `mcp` map of typed local-server entries. And **032 signposted 028 but not 031**, despite declaring 031 a dependency, citing it six times, and adding a fourth row to the per-agent descriptor 031 introduced — so 031 now carries the matching `Signpost (post-032)`. **The SIMPLICITY waiver expired on the merits**, which is the outcome the waiver-handling warning does not describe: it excused `scope` as derivable metadata because only `mechanism` drove branching, and §MCP wiring now splits on `scope` itself (project-committed ⇒ repo-relative pointer, user-global/home-level ⇒ absolute store path). `process-waivers` ran unrestricted, reported 1 expired / 0 applied / 0 retained, and the frontmatter entry was removed with the reason recorded in the Summary — expiry is only a hazard when it is *silent*. **advisory 0 after excluding the documented false positive by hand, and the count was 4, not 2**: `check-orphaned-references` reported `AGENTS.md -> specs/rules/` twice plus `specs/rules//` and `specs/templates/`, all on lines 119-120. It went **2 to 4 inside `dd8ce2a3`**, the commit that corrected that very AGENTS.md entry — so "expected two" would have filed two real-looking findings. AGENTS.md's stated count is corrected to four in the same commit. **047 closed 2026-09-13, and it is the corpus's worked example of a rejected option a spec adopted *inside itself*.** Recorded 0/0/0, examined 7 of 8, unexamined **1** — up from 0, and caused by this pass's own edit. Step (5): pre-reopen natural base `6c40644d` at **315** modified-since / 315 in scope against a plan affecting **4**, collapsing to **4 / 8** on `c5259680`; `--since HEAD` gave 0 / 4 and was declined. Both legs returned inline (917 bytes), so no friction asymmetry. **Four defects, all fixed in the pass, none captured** — the inbox stayed at 12. The headline one is entirely intra-spec: `scenarios/analyze-run-durability.md` resolved *should the gate also check analyze freshness?* with **not yet**, and its sibling `scenarios/analyze-record-freshness.md` then built exactly that gate, answering the learned-ignored objection by removing its cause (a content comparison against `analyzed-digest` instead of the sha diff). The freshness scenario cites **022's** scenario as where the superseded reasoning lives and never mentions the sibling under its own spec carrying the identical decision — so a live *not yet* sat over a shipped gate (AC13-AC15, `check_review_gate.rs`'s `AnalyzeStale`). Two count words went stale with it: `check-review-gate` "gains **two** block reasons" (it carries three) and the review analyze row "in all **three** states" (four — the scenario contradicting its own Edge Cases). Plus the spec's first Resolved Question rejecting an `analysis.md` partly because *analyze findings drive no gate*, which its own AC9-AC10 falsified. **Two verification traps worth carrying forward.** AC12's four states must be compared against the **rendered** labels in `framework/commands/review.md`, not the `RecordFreshness` variants, which are named differently (`never-run`/`current`/`stale`/`undeterminable`) — comparing the wrong pair manufactures a defect. And AC10's ordering must be read from `run()`'s control flow, not source line order: `ReviewStale` is *defined* at line 423, after `AnalyzeStale` at 376, and reads as a contradiction until you notice both are helpers rather than call sites. **`unexamined` went 0 to 1 and the entry is the pass's own**: the corrected bullet quotes the phrase *not yet*, an implementation-state tell, in a block linking to the sibling scenario — and a scenario carries no lifecycle status, so `link-adjacent-drift` records `no-readable-state` rather than guessing. The family behaving exactly as `analyze.md` specifies, not a defect; left in place rather than reworded, since driving the count to zero by re-running a review is optimising the metric. **advisory 0 after dropping the same four `check-orphaned-references` findings by hand.** **Family 37 verified directly: 54 done specs examined, backlog 0 against baseline 0** — the grandfathered set is fully drained and the ratchet closed, so AC11's bounded-and-shrinking claim holds. **Still open and NOT taken here:** `framework/commands/analyze.md` §Spec integrity's minimum-criteria check is in 047's review scope and is the subject of its own inbox item below, which already carries the operator decision that zero criteria are valid at `draft`. One markdown edit, reopens no spec — deliberately left to that item rather than bundled into a backfill commit. **Two closures on 2026-09-13 that were NOT campaign passes, recorded because they moved the inbox and the corpus.** `ductus-v0.49.4` shipped the three-item release batch (above). Separately, **020's embedded-snapshot item was discharged** under its recorded operator decision: `spec.md` went **803 → 275 lines**, the three frozen command-source copies replaced by a pointer. It cost **two** reopens, not one — 050's scenario `a-canonical-source-is-pointed-at-not-copied` records that copy as the pointer rule's motivating instance **in the present tense**, so removing the copy falsified it and it was corrected by tense. Both specs are back at `done` with 0/0/0 and refreshed digests. The removal also surfaced three defects nobody had reported: 020's `data-model.md` and `plan.md` both pointed at the deleted section (the plan calling the embedded copy "the canonical content", backwards under the canonical-sources rule), and 020's authoritative-shapes tables were missing **six** fields the runtime writes (`examined`, `scope`, `reviewed-digest`, `captured-issues`) — contradicting 020's own AC15. All in `AGENTS.md` §Design Principles now. **Inbox went 12 → 7 across the session** (zero-criteria-at-draft, the three release items, and 020's snapshot). **Two things about analyze runs changed permanently with 0.49.4 and every later pass should expect them:** `check-orphaned-references` now reports `findings: 0` in this repo, so the four-finding hand-exclusion and its "say how many you dropped" commit line are **retired** — if it fires again, that is a real finding; and `check-rule-ids` reports `examined`, so a `missing` list is interpretable without recalling what the caller passed. **For 029's pass (third on the Remaining list), measured here rather than left to be rediscovered:** its §MCP Wiring, AC6 and AC16 still state the layout-derived model as current ("`.mcp.json` for `claude-style`, `{config_dir}/mcp_config.json` for `antigravity`"), and its `Signpost (post-031)` — correctly placed by 031's task 4 — sits ~60 lines above them and says only `write-file` agents are Claude, which 032 made false. Not fixed here: editing 029 would reopen it and stale a review its own pass re-runs anyway. **Two procedure details 008 added, both cheap to miss.** (1) **A spec's own `review.md` enters its own review scope** once the review commit lands inside the diff window — 008 went 20 → 21 that way when the review was re-run after a follow-up fix. It is the record the run writes, not a subject its passes read, so name it in the Summary and do **not** count it in `examined`; counting a review's own output as a file that review examined inflates the numerator with its own artifact. A third, smaller: **the Summary is linted after it is written, not before** — `write-review` renders it into `review.md` and `markdownlint` reads it there, so a code span with a trailing space (`` `loading rule files: ` ``, MD038) fails the gate one step *after* the write and costs a re-run. Write Summary prose lint-clean, then re-run `lint-markdown --path specs/{feature}` before committing. (2) **`unexamined` measured at `done` does not always grow after a pass** — 028's lesson was that a pass's own annotations add backticked paths and push the count up, and 008 is the other outcome: it replaced brace-bearing paths (invisible to `is_path_like`) with backticked ones carrying an interior slash, and the count stayed at **6** because the new paths duplicated candidates already in the set, which the `skipped` array deduplicates. Neither direction is predictable, which is the actual rule — flip the status, re-run `check-artifacts`, record what it returns, and walk the array by hand regardless. **005 left the campaign rather than completing it**: its backfill pass opened on 2026-09-13 and found that 043 had removed the whole workflows feature, so [§spec-lifecycle](../framework/constitution.md#spec-lifecycle)'s *a retired feature's spec is deleted* applied. Operator confirmed; 005 was consolidated into 043 and its directory removed. The corpus is **54 specs**, not 55 — every count on this item that predates 2026-09-13 is against 55. **029 closed 2026-09-13, and it is the campaign's worked example of a spec describing two reversed models as current.** Recorded 0/0/0, examined 15 of 17, unexamined 0. Step (5) as predicted and then some: the pre-reopen natural base resolved **549** modified-since / 551 in scope at **117,508 bytes**, and the step-4 commit collapsed it to **12 / 17 at 4,911 bytes** on base `0d251acc`; `--since HEAD` gave 0 / 8 and was declined for excluding the nine files the pass edited. **Both legs returned inline post-reopen**, so the `jq` route was needed only for the pre-reopen measurement — the friction asymmetry is a property of the pre-reopen window, as 031 also found. **Thirty corrections, all fixed in the pass; one captured.** The two headline defects were the known ones plus a bigger one nobody had measured. **MCP wiring**: §MCP Wiring, AC6 and AC16 named a per-layout path out of `ductus.md` §Derived values, which carries no MCP row since 031 made discovery per-agent; 032 added OpenCode as a **second** `write-file` agent, so the Signpost's "only `write-file` agents (Claude)" and "`surface-instruction` agents (Auggie)" were each one agent short, and Antigravity gets **no** MCP file written at all. **State B no longer aborts** — this one was not in 031's measurement: §Pre-flight Phase, §State B, AC11, AC15 and the Post-completion note all described a single combined pre-flight abort before the archive fetch, which 048's scenario `state-b-continues-in-session` removed. **AC11 asserted the opposite of shipped behaviour while ticked** ("No archive is fetched and no scaffolding runs"), and `check-artifacts` was clean the whole time — a criterion whose paths all resolve while its claim is false is invisible to the family, which is §Gotchas' rule meeting its worked example. **README was correct throughout** and is what the corrections were checked against — the outlier-is-the-drift tell again, as with 032 and 008. **Three of the four scenarios needed edits**, sequenced ahead of the review: `state-a-deterministic-path-forcing.md` instructed a State-A run to call `substitute-templates` and `merge-claude-md`, retired by 022 task 68 (`70420f98`) and absent from `framework/runtime-tools.txt`, so there was no tool to call and `ductus.md`'s own State-A list already carried the corrected set; `runtime-probe-parity-audit.md` said Family 15 hard-codes "the three current agents" where the script carries four; `project-inputs-asked-once.md` named State-B wiring as an abort cause and State C as live. **Fourteen dead `§` references** across those three scenarios, plus two in `plan.md`, named sections of `ductus.md` without naming the file on the reference's own line — pre-existing, verified against HEAD before touching them, and all now `unresolved: []`. **`plan.md` carried a resolved decision still recorded as open** (the Antigravity probe grammar; `command(which)` had shipped in both the seed and the configure file, with Family 15 guarding the pair) — §drift-prevention's *Decision resolution* in its purest form, and the second campaign pass to find one in a **plan**'s §Trade-offs after 003's. **Four framework-side fixes, reopening nothing**: `framework/constitution.md` §runtime-host-integration and `ductus.md` §Detection mechanism gave the host tool-prefix mapping for three agents, so an **OpenCode adopter matches neither spelling and concludes the runtime is absent, re-acquiring every run** (032 verified the `ductus*` prefix glob over `<server>_<tool>` names and left the separator unpinned, so the correction names the prefix); `ductus.md`'s runtime tip still said State B "aborted in pre-flight before this output", contradicting §Pre-flight abort and §Closing restart in the same file; the State-A contract still said "State-B/C fallback specification"; and Family 14's header enumerated three permission formats against four. **`unexamined: 0` was verified, not trusted** — of 31 backticked spans in the criteria only three carry an interior slash, and the brace-bearing `configure/{key}.md`, the slash-less `ductus.md` / `.mcp.json`, the three spec slugs and the `state-b-continues-in-session` scenario were each checked against the tree by hand. **`check-orphaned-references` reported `findings: 0`**, so the retired hand-exclusion stayed retired and advisory 0 is a real zero. **One capture, inbox 7 to 8**: 022's `framework-list-dedup.md` says it records an open question its own Open Questions section does not carry, and `configure/{auggie,antigravity}.md` send adopters to that empty home — the principal fix is a durable contract under a closed spec, so decision (a) binds and it is logged with three shapes priced. **Measured for 048's own pass, so it is not rediscovered:** 048's §Detection states bullet 3 still says the former State-C path joins "the same single combined **Pre-flight abort**", which its own **AC10** contradicts in the same file ("the run then completes its remaining work through the CLI in that same session, and the single closing restart"). **Measured for 045's own pass:** natural base `c27a08f4` at **397** modified-since / 402 in scope against a plan affecting **17**, payload **96,366 bytes** — under the cap but wide; artifacts total **711 lines across 5 files**, no `scenarios/`, but a 165-line `data-model.md` that [§drift-prevention](../framework/constitution.md#drift-prevention)'s canonical-sources map designates THE source for the open-state tell list and the decision-drift grammars, so any correction there is the canonical-sync class that turned Family 19 red on `main` mid-release in 0.49.4. Price that before starting. **Per-spec procedure.** (1) `check-artifacts` and read the `skipped` array — every entry is a criterion nothing has ever substantively checked, **and an empty array is not the complement of that**: `criterion-path-existence` only sees a backticked path carrying an interior `/`, and `scenario-consistency` does not run on `done` specs at all, so `clean: true` with `skipped: []` can sit over both a dead path and a task naming a scenario file that does not exist — 007 carried one of each for four months (§Gotchas). (2) Read the spec's criteria and verify each against the tree, **enumerations included** — counts, route lists and parentheticals are where this campaign's defects have actually lived. Then read §Trade-offs, §Non-goals and the Resolved Questions the same way: a **decision** goes stale as silently as a count, and a rejected alternative that the repo later adopted is invisible to every check (§Gotchas). Ask of each: does the repo still behave this way, and do the premises the decision rested on still hold? (3) Check `README.md`, `AGENTS.md`, `CLAUDE.md`, `docs/` and the shipped `framework/templates/` for mirrors of the same claim. (4) If a correction is needed: `set-status` → fix → commit; else the spec never leaves `done`. (5) `compute-review-scope` **twice — once with no `since` and once with `--since HEAD` — and record both counts before choosing**; the natural base is often a window spanning months against a plan affecting a handful of files, and AGENTS.md §Gotchas says `HEAD` is the answer in exactly that case. **Measure after step 4's commit, not before.** A spec reopened and committed in step 4 records a fresh `in-progress` transition, so the natural base moves forward to that commit's parent and the window usually collapses to the plan's surface plus whatever the pass just edited — no override needed, and a tighter *and* more honest denominator than `HEAD` gives, because it covers the edits this pass made rather than excluding them. 052 measured 321 files on the pre-reopen natural base, 7 on `--since HEAD`, and 9 on the post-reopen natural base, which is what it recorded. Read the scope, then `write-review` with a truthful `examined` and a Summary naming every unread file and *why* — including in-scope paths that no longer exist, which stay in scope because the plan lists them. (6) commit, run the analyze families, then `write-analysis`, `set-status` back to `done`, commit. **Measure `unexamined` at `done`, never at `in-progress`** — `criterion-path-existence` does not run on an in-progress spec, so `check-artifacts` reports `skipped: []` there and a count taken before the flip understates it. Flip the status first, re-run `check-artifacts`, and record what it actually returns. 028 is the worked example: the prior record said 3, the pass's own AC5 annotation added a fourth backticked path carrying an interior `/`, and the true count at `done` was **4** — a predicted 3 would have been wrong and nothing downstream would have caught it. Setting the status before `write-analysis` is also the safer order for the digest: it makes `analyzed-digest` match the final file exactly instead of relying on `check-review-gate`'s already-done arm to absorb the later flip. The original ordering note, which still holds if you flip afterwards: `write-analysis` digests `spec.md` with its own `analyze:` block excised, and the completing `set-status` is what `check-review-gate`'s `already-done` arm absorbs, so the flip does not stale the record (verified on 052, and 050 did the same). **`validate-frontmatter` is spec-path only** (`analyze.md` step 2): pointed at a scenario it reports `status` and `dependencies` missing for *every* scenario in the corpus, which is the primitive being misused rather than a finding. **Sequencing decision 2026-09-13 (operator): the three corpus-wide sweeps run BEFORE this campaign resumes.** **Residue deliberately deferred into three of the remaining passes (2026-09-13):** the common-noun sweep reverted its edits to three **durable contracts** — `specs/000-slash-commands/scenarios/command-autocomplete-summary.md` (1 line), `specs/005-workflows/data-model.md` (1 — **moot**: 005 was consolidated into 043 on 2026-09-13 and the file no longer exists), and `specs/013-text-first-artifacts/data-model.md` (3) — because `scenarios/*.md` and `data-model.md` are the only files a review's freshness keys on, so editing them staled all three reviews and turned `/ductus:audit` Family 19 red on `main`. All three specs are on the Remaining list above and their backfill pass re-reviews them anyway, so the six lines are cheapest to sweep **during** that pass rather than before it. Sweep them when you open 000 and 013 (005's is discharged by its removal); the rest of the common-noun sweep is landed and its item retired. **The other two sweeps landed 2026-09-13 (`d9c1d784`) and deferred three more durable-contract lines here on the same reasoning** — `specs/000-slash-commands/scenarios/validation-gates.md:21` (the lightweight track's two-filename fallback, stated as live behaviour) and `specs/013-text-first-artifacts/data-model.md:13,:27` (the frontmatter schema's file list naming `spec-and-plan.md`). Both specs are on the Remaining list and both already carry deferred common-noun lines, so 000 and 013 each have **one** durable-contract edit to make when their pass opens them, not two. Nothing else is owed by either sweep: they touched only `spec.md` and `plan.md`, so no review went stale — `022/scenarios/anchor-reference-kinds.md`, `051/scenarios/the-numbering-grammar-reaches-every-surface.md` and `017/data-model.md` were each read and judged **correct as written** rather than deferred, and `023/scenarios/living-specs.md` is the record of the removal and is off-limits. Process note worth keeping: the pre-commit audit was green because Family 19 reads *committed* history — a history-reading family must be re-run after the commit, which is the rule `AGENTS.md` §Workflow already states. Those are the common-noun product-name sweep, the frozen-archaeology present-tense sweep, and the lightweight-track / `spec-and-plan.md` sweep, each an item of its own below. All three record the same stance in their own text — the replacement varies by sentence, so it is **not** a uniform substitution and each affected spec takes the back-edge. Per `AGENTS.md` §Workflow a non-uniform diff does not earn `mechanical_sweep`'s exemption, so the reviews on the specs they touch go stale. Measured 2026-09-13: the three name **17 distinct already-backfilled specs** (001, 002, 003, 006, 007, 009, 014, 016, 017, 018, 020, 021, 024, 027, 033, 049, 050) and **7 of the then-remaining 20** (000, 005, 008, 012, 013, 019, 023). The 17 reopen once whatever the order — that cost is sunk — but backfilling the 7 before the sweeps lands a review against a body about to change, so each would be reviewed twice. Sweeping first costs 7 duplicate review+analyze cycles less and nothing more. **Apply the sweeps grouped by spec, not by item:** the three overlap (007, 017, 020, 024, 000, 013, 023 appear on more than one), so a spec hit by two or three takes one reopen carrying all of them, then one re-review and one re-analyze — the same rule this campaign already states as fix-inside-the-spec-you-have-open, applied to the sweeps themselves. Resume at the head of the **Remaining** list above afterwards (naming a fixed number here goes stale on every pass). **Standing operator decisions (2026-09-13):** **(a) Do not revisit a spec the campaign has already closed.** Stated 2026-09-13 while grooming. A finding whose fix lands inside one of the **Done** specs above waits — for that spec's own later pass, for a deliberate decision to spend the reopen, or for the finding to be routed somewhere that is not a spec. This binds `/ductus:groom` hardest: the scenario route **reopens** its matched spec, and a scenario is a durable contract, so routing one into a closed spec costs a reopen **plus a full five-pass re-review**. Check the Done list before choosing the scenario route. A chore whose fix touches only `framework/` reopens nothing and is done in the pass — that is the distinction that let a groom pass on 2026-09-13 take the inbox 18 → 14 without touching a spec. **(b) Grooming stance: fix chores, route scenarios.** Same conversation. Do not park a chore; §brownfield-inbox and `AGENTS.md` §Workflow already say so, and (a) is what makes it tractable — sort candidate fixes by *what they touch* before sorting by what they are. **(c) The release-blocked batch — four of five shipped as `ductus-v0.49.2` on 2026-09-13.** Five items were `runtime/` edits each carrying a version bump and a `ductus-v<version>` tag, so batching them was one tag rather than five releases. **Shipped in `ductus-v0.49.2`:** `retire-feature`'s `# Errors` clause; `ArtifactFinding::family`'s eight-for-nine enumeration; `CheckArtifactsResult::findings`' sibling of it (plus `ArtifactFinding::severity`, a **third** site of the same defect that none of the three items named, found by reading the struct rather than grepping); and the `.govern/` third tier in runtime doc comments, whose two **functional** halves — Family 12's find missing `*/.govern/session.toml` and Family 17's two-tier config probe — were each proven red before the fix and are the reason that item was worth doing. Inbox 14 → 10. **Not shipped: the `[constitutions.*]` validation gap, and the reason is a measurement (c) did not have.** The validation itself is small — three value checks the bootstrap already specifies (alias a bare TOML key, `repo` URL-shaped, `path` non-empty). Its cost is not the code: per `AGENTS.md` §Workflow's runtime-home rule the fix lands as a scenario under `022-deterministic-runtime`, which reopens 022, and a scenario is a durable contract, so it obliges a full five-pass re-review. **022's review scope measured 246 files / 222 modified-since on 2026-09-13** (natural base `d041432c`), against the 77/53 `AGENTS.md` §Gotchas recorded on 2026-09-12 — that entry's own warning that its figures are evidence about the day they were taken, borne out. A reopen-and-commit would collapse the window per step (5), but the pass is still the largest single re-review in the corpus and is its own unit, not a doc fix batched into a release. **That deliberate decision was made the same day: the operator said do it.** The 022 reopen was authorised, spent and closed on 2026-09-13 — the validation shipped as 022's scenario `the-constitutions-registry-validates-its-values` with task 115, and 022 returned to `done` with a 0/0/0 review (examined 10 of scope 41) and a 0/0/0 analyze (unexamined 2), released as `ductus-v0.49.3`; its own inbox item is retired. The reopen collapsed 022's review window from the **246 files / 222 modified-since** measured on the pre-reopen natural base to **41 / 14**, which is step (5) borne out — `--since HEAD` would have given 33 / 0 and was declined because it excludes the pass's own edits. Decision (a) was satisfied for that one spec by an explicit override; it still binds every other closed spec. **The release batch's three members SHIPPED as `ductus-v0.49.4` on 2026-09-13, taking the inbox 11 → 8.** They were: the `[services]` duplicate-repo identity mismatch found reviewing 030, plus two defects that had been recorded only as `AGENTS.md` §Gotchas workarounds and were captured properly the same day — `check-rule-ids` reporting no denominator, and `ships_to_adopter`'s trailing-slash miss. The reasoning that had kept those two out was a misreading of §brownfield-inbox's no-hold-state rule, which discards items blocked on something *outside* the project; both are small in-repo fixes deferred by **cost**, evaluable today, and an AGENTS.md entry is a workaround surface rather than a queue — nothing routes it and nothing surfaces it at a gate. Decision (c)'s lesson held: they were one release session, not three chores, and batching them is what made them tractable. **Two things the pass established that the items did not know.** `ships_to_adopter`'s "latent half" was not latent — `criterion-path-existence` passes an already-trimmed span, so it was never reachable by the defect, and `check-orphaned-references` was the only live call site; the 19 trailing-slash spans across 11 specs could not have misfired. That is the second time this one function has been characterized wrongly in `AGENTS.md`, which is why the fix normalizes *inside* the helper rather than at either caller. And the `[services]` fix is one identity function shared by the harvester and the duplicate detector (`schema::services::normalize_repo`), because the defect was two notions of "the same repo" rather than a missing check. All three regressions were proven red before the fix. The documented `check-orphaned-references` false positive is **gone** — this repo now reports `findings: 0` — so the manual-exclusion workaround and its "say how many you dropped" instruction are retired from `AGENTS.md` §Gotchas rather than carried. **The next unit is this campaign, resuming at 029.** Identify the item by subject, not by grep: `version bump` also matches this item and the adopter-name one. **stop when a spec's analyze record is committed and pushed, then judge whether to go on — the limit is remaining session capacity against the next spec's size, not a fixed count per session.** Clarified by the operator 2026-09-13, after a session offered to stop following a small spec: *"not every spec is large enough to warrant a new session. there is plenty of context remaining."* The rationale below is unchanged and is what the judgment is made against — it argues from degrading judgment, not from a quota, so a large spec late in a long session is the case to stop on. Still not "one spec plus whatever adjacent work looks cheap": a session that ran three in sequence fixed a superseded rationale in one spec and an hour later declined the identical fix in another by inventing an exemption the rules do not have, missed that a rejected alternative had been adopted in a file it had just reported as fully verified, and left an adopter-halting release unnoticed for hours with the evidence in its own context the whole time. The mechanical steps stay fine as a session lengthens; the judgment ones degrade first, and `write-review` records both outcomes byte-identically, so nothing downstream can tell. Second instance, 2026-09-13: a session that ran 035, then 050, then 007 recorded 050's review against a HEAD whose Summary described an `AGENTS.md` edit that had never been staged — the mechanical half (scope measured on all three bases, every gate run, every commit `git show --stat`-checked) was clean throughout, and the judgment half is what slipped. Per-spec reopen rather than batched; fix inside the spec you have open and log a cross-spec pattern as one measured inbox item instead of sweeping piecemeal; fix defects in shipped adopter files inline at source; a MUST or SHOULD is fixed or waived-with-reason before the spec returns to `done`, never carried. Parked rather than done in one pass because it is per-spec judgment, not a mechanical edit (captured during the 2026-09-13 systematic re-review)
- [ ] convention: grounding — a `done` spec corpus, two rule files and the runtime record another adopter project's name, against §Workflow's "never record another project's name in this repository — describe the shape instead". Measured 2026-09-13: ~100 occurrences. They split three ways and only the first is a straight sweep. (1) **Spec prose** — `000` (spec + plan, 6 hits), `001` (spec + plan, 3), `002` (spec + plan + tasks, 5), `017` (2 scenarios), `022` (6 scenarios), `036` (spec + plan + tasks, 4): each states *what kind of project* it derived from or was motivated by, so the name drops out with no loss of meaning. (2) **`framework/rules/quality-cross.md`** `QUAL-STUB-001`'s Rationale and `framework/migrations/{ductus-rename,session-file-consolidate}.md` — these **ship to adopters**, which makes the leak worse than in a spec body. (3) **`runtime/`** — source doc comments, `CHANGELOG.md`, and test fixtures that use the name as a *placeholder project* (`tests/fixtures/exec-auggie/`, `exec-opencode/`, `parity.rs`, `host.rs`): a fixture is the sanctioned form per that same entry, so whether these count is an operator call, and changing them is a `runtime/` edit that needs the version bump and `ductus-v<version>` tag. Note the entry's own warning: the replacement text varies per sentence, so this is **not** a uniform substitution and does not earn the mechanical-sweep exemption — each affected spec takes the back-edge (captured during /ductus:analyze of 001). **Tier (2) is DONE (2026-09-13)** — the shipped half, which the item itself called the worse leak: `framework/rules/quality-cross.md` `QUAL-STUB-001`'s Rationale now says *an adopter project running a backend service*, `framework/migrations/ductus-rename.md` says `/{project}:specify`, and `framework/migrations/session-file-consolidate.md`'s two examples use a placeholder `acme`. `git grep -i anvil -- framework/` is now empty, so nothing adopters receive carries the name. **Tiers (1) and (3) remain, and each is blocked on a different thing.** (1) spec prose in 000, 001, 002, 017, 022, 036 — every one except 000 is already backfilled, so sweeping them reopens specs the campaign has closed; operator asked on 2026-09-13 not to revisit processed specs, so this waits for a decision or for 000's own pass. (3) `runtime/` doc comments and fixtures — an operator call on whether a fixture name counts, and a `runtime/` edit either way, so it carries the version bump and tag. **Second name folded in from a separate item (2026-09-13):** `svc-zmc-api` was never measured by the tier list above and lives in `specs/045-decision-state-drift-detection/spec.md` (lines 42 and 172, the observed-case table and Prior art) and in `specs/046-scenario-open-question-visibility/`. Both 045 and 046 are backfilled, so it falls under the same tier-(1) hold. Merged here rather than carried separately — one name-leak item with the full spread, not two discovered twice.
- [ ] bug: `specs/022-deterministic-runtime/scenarios/unreadable-scenario-is-reported.md` contradicts shipped behaviour on two sentences, and a scenario is a durable requirement document rather than a note. It states **"Nothing gains a block"** and, of the `check-artifacts` skipped record, **"It is not a finding: the file is an unknown, not a defect."** Commit `8c4ca74` (*feat(022): artifact-unreadable blocks a done spec*) reversed the second half: `check_artifacts.rs` now documents and implements *"**artifact-unreadable at `done`** (blocking, across families) — a skipped target below `done` and a **blocking finding at it**"*, and `framework/commands/analyze.md` §Unexamined targets carries the matching exception, naming `scenario-open-questions` as the motivating family. The first half is still exactly right and must not be swept with it: `check_review_gate.rs:803` still yields no block for an unreadable scenario, deliberately, so the gate/finding split is the whole point and a blanket edit would destroy it. Measured 2026-09-13 while re-reviewing 046, whose AC22 carried the same reversed claim and is now annotated. Fix is surgical — two sentences in the scenario's §Behavior, plus its Edge Cases bullet on the `link-adjacent-drift` double-record — but it edits a durable contract under a `done` spec, so it takes 022's back-edge and stales 022's `reviewed-digest`, which is why it is logged rather than folded into 046's pass (captured during /ductus:analyze of 046)
- [ ] convention: .claude/commands/ductus/init.md is an orphan that no sweep reaches, and it has drifted across five specs' renames. It is the one hand-maintained file under .claude/ (AGENTS.md §Gotchas) — no framework/commands/init.md source, so no generator rewrites it; and .claude/ is deliberately absent from AGENTS.md's sweep-target manifest, correctly, because every other file there is generated output. init is also absent from help.md, README.md, docs/slash-commands.md and scripts/maintainer-only-commands.txt (which lists only `audit`), and Family 16 anchors on framework/commands/*.md source files so it never sees init either. Measured 2026-09-13, five drifts: (1) it copies 6 of the 11 shipped rule files — concurrency-backend, observability-backend, performance-backend, quality-cross and reliability-backend are missing, so a scaffolded project silently gets a smaller rule set than /ductus installs; (2) it copies a spec-and-plan.md template that 023 deleted; (3) it writes the constitution to repo-root constitution.md, the pre-044 location, not .ductus/constitution.md; (4) it writes a new project's config to .govern.toml at two sites — a write to a fresh tree, not a read of a legacy tier, so it scaffolds straight into the layout the migrations exist to undo; (5) it creates .claude/{slug}-session.json, while the session file is .ductus/session.toml for every adopter, project-name-agnostic and TOML. It also never acquires or wires the runtime, which §runtime-boundary principle 3 now makes required. This is the drift §drift-prevention's Manifest discipline exists to prevent, and that rule names this exact pair as its example — "/ductus and /{project}:init both scaffold a project" — so the framework constitution ships adopters a reference to a command they never receive. Repair-vs-retire is an operator call, not a sweep: /ductus supersedes init for every adopter, and init's only remaining role is scaffolding a brand-new project locally. None of this falsifies a 004 criterion — all nine hold. — `.claude/commands/ductus/init.md` (captured during review of 004-tech-stack-selection). **Operator decision 2026-09-13: retire it.** `/ductus` already covers the greenfield case — `framework/bootstrap/ductus.md` names "greenfield run" and "a fresh project where none exists", needing only `git init` first — so init's one remaining role is already served and repairing five drifts would preserve an orphan that drifts again on the next rename. Delete the file, and update the constitution's §drift-prevention Manifest-discipline example, which is the only live reference to `init` anywhere in the repo; that is a 050 back-edge
- [ ] bug: a non-zero `analyze.advisory` count with no corresponding inbox capture is undetectable. `analyze.md` step 16 is what persists a finding's *content* — an `append-inbox` call per surviving finding, before the render — while `write-analysis` records only the *count*, and the constitution's §brownfield-inbox says both capture paths MUST be captured rather than dropped. Nothing holds the two against each other. The review half has `check-review-agreement` (Family 31) pinning `review.md` to its frontmatter; analyze has no counterpart, because it writes no report artifact for one to compare against — so a run that records `advisory: N` and appends nothing is byte-identical to one that captured all N, which is `QUAL-CLAIM-001` turned on the analyze record itself. **Measured 2026-09-13.** Every per-spec run checks out: the four advisories recorded on 2026-09-13 are all accounted for — 001, 011 and 046 each have a live inbox item carrying its `(captured during /ductus:analyze of NNN)` marker, and 002's was correctly *not* re-appended because it was the adopter-project-name class already filed under 001, which is what the `dedup-prefix` guard exists to do. The one divergence is a **batch** run: `7963f134` (2026-09-06, "drain the analyze-record backlog to zero") wrote 50 analyze records totalling `advisory 21` and touched `specs/inbox.md` not at all — confirmed by `git show --stat` on it and on its sibling `0405f6f7`. That instance was benign and deliberate rather than a slip: its commit message identifies the findings as one class (prose-qualified `§` references to the installer's sections written without naming `ductus.md` on the line, plus 023's two historical anchors), records the decision to carry rather than chase them across six back-edge cycles for findings that by construction do not block, and the class later got a durable home in `AGENTS.md` §Gotchas naming 023, 029, 031 and 032. So this is not a lost-content report — it is that the same skip performed carelessly would look exactly the same. Ten of those advisories still stand in frontmatter today (032:3, 031:3, 029:2, 023:2). Routing is `/groom`'s call, not this item's; the shapes on the table are an analyze-side agreement check (the `check-review-agreement` counterpart, which needs something durable to compare against), or making the recorded count derivable from the captures rather than supplied alongside them. Found during a post-close survey of work tracked outside the inbox, not during an analyze run — so no capture marker applies
- [ ] convention: second promotion round — audit `AGENTS.md` against `framework/constitution.md` and move everything adopter-beneficial into the constitution, consolidating anything stated in both to the constitution with an `AGENTS.md` link. **Operator request 2026-09-13**, stated as two halves: (a) everything currently in `AGENTS.md` that can benefit adopters moves to `framework/constitution.md`, which is the one artifact adopters receive; (b) any instruction living in *both* is consolidated into the constitution, with `AGENTS.md` keeping a link. **Measured 2026-09-13.** `AGENTS.md` is 148 lines / 135,952 bytes carrying 103 top-level bullets: 92 rule-bearing across the four sections 050's plan scopes (Workflow 44, Gotchas 40, Boundaries 3, Design Principles 5) plus 11 descriptive (Project Structure 6, Tech Stack 5) that 050 correctly held out of scope. **41 entries cite a `framework/constitution.md#` anchor; 62 cite none.** Of the 41, only 31 use the established mirror phrasing (26 "contributor-side mirror", 5 "contributor-side record"), so roughly 10 cite the constitution without declaring themselves mirrors — which is exactly where half (b)'s duplication hides, since a restatement that happens to link is indistinguishable from a pointer by grep. The constitution is 760 lines across 32 anchored sections. **This is 050's territory and needs no new method.** §drift-prevention's canonical-sources map names `specs/050-constitution/spec.md` as the home for constitution-content work — "what belongs in it, how it is organized, which rules adopters receive" — and 050 already supplies every instrument this needs: the **reword test** (promote iff the rule restates without naming machinery only this repo has *and* without losing what makes it actionable), the three-tier **Classification** (universal → constitution; shared → an org's registered `[constitutions.*]` document per 055; project-only → stays), and **§Promotion mechanism** (one normative statement in the constitution, one `AGENTS.md` line that points and states nothing of its own — which is half (b) exactly). 050's plan already anticipates this run: "the table is a snapshot of one pass, and a later promotion round re-derives rather than amends it." **Root cause — do the flow fix first, before the 92-entry pass.** The backlog is not neglect and 050's AC1 did not cause it: AC1 correctly rejected a per-entry marker, since a field an author must remember to fill is the diligence dependency §design-principles rejects. The cause is that **every write-time routing rule discriminates by *kind*, while promotion discriminates by *population*.** Three sites tell an author where a learning goes and none asks the universality question: `AGENTS.md:5`'s header says only "append durable learnings to the matching section"; `CLAUDE.md`'s Auto-Memory Routing asks "would this help any other contributor **to this project**?"; and the constitution's §drift-prevention *Shared knowledge stays in git* answers by kind — *"A **project learning** — a convention, a gotcha, a workflow rule, a boundary — goes in `AGENTS.md`."* So a **universal** gotcha lands in `AGENTS.md` **because it is a gotcha**, not because anyone judged it project-only, and the population test that would catch it (050 §Classification: "one question about the rule's population") lives in a spec nobody reads while writing one. The default manufactures the backlog continuously — 54 rule-bearing entries at the survey, **92** now — so running the 92-entry pass without changing the routing just resets a counter that refills. **The fix is not a new authored input**, which is why §design-principles does not bar it: an author already chooses a destination, and changing *which question decides it* adds no remembered step. Move 050's population question into §drift-prevention's *Shared knowledge stays in git* bullets, and mirror it at `AGENTS.md:5` and in `CLAUDE.md`'s routing block — three small edits plus one 050 back-edge, landing **before** the bulk pass. **Correction to the counts above, which are proxies rather than measures.** Neither grep is the answer: the 62 non-citing entries **overcount** the backlog because 28 are settled project-only from 050's pass, and the 41 citing entries **overcount** the promoted set because project-only entries cite `§spec-lifecycle` and friends for context — 41 against the 26 that pass actually produced. A sound coverage check needs 050's classification table as an input, **re-keyed from positional IDs (`W4`, `G11`) to each entry's bolded lead phrase**, since positional IDs drift the moment an entry is inserted and the table is already stale by that mechanism alone. Then *unclassified = rule-bearing − (table-keyed ∪ constitution-citing)*, a new entry is unclassified **by default** (the honest state, and the opposite of a field that silently stays empty), and the number today is **~34**, not 62. Render it as a standing notice on the §brownfield-inbox inbox-row precedent — **notice, never gate**, because promotion has to stay free or the honest choice between a growing backlog and a silent one pushes toward silence. The re-keying is work this round must do anyway. **Honest limit on both halves:** neither makes promotion happen; one stops new misrouting and the other makes not-having-promoted visible. The reword test stays human judgment, which §runtime-boundary principle 2 keeps off the runtime regardless. **Scope note for whoever takes it:** this is a spec cycle of its own, not a session tail — 92 entries judged individually, each promotion reworded for adopter-neutrality with its reason recorded (AC1's audit trail), and every promoted entry's `AGENTS.md` line rewritten to a pointer. Expect the constitution to grow materially, so §Resolved Questions' bullets-not-sections rule and AC10's anchor-stability constraint both bind. Two constraints already settled and not to be re-litigated: a constitution-only change moves no version pin and needs no tag (050 §Resolved Questions), and the mirror stays in `AGENTS.md` rather than being deleted, because contributors read it as the index of how to work here and a rule that silently left reads as a rule that was dropped. Routing is `/groom`'s call, but the back-edge onto 050 is the obvious shape
- [ ] convention: `framework/bootstrap/ductus.md` is 141 KB and could be split at the **archive boundary** for a ~40% cut to what adopters install — but not at the agent boundary, which is mostly already done. **Measured 2026-09-13, whole file read (1182 lines / 140,985 bytes).** Composition: **90.2% prose**, 7.0% tables, 2.8% fenced blocks, so the cheap theories all fail on measurement — the shell fallback assumed to duplicate the deterministic path is **2.8%**, the registry's four inline JSON permission blobs are **2.5%**, and retired-release tokens (`.govern/`, `.govern.toml`, `gvrn`, "retired", "pre-04x") total **14 lines** whose config-ladder tiers are load-bearing per §Gotchas. A dead-code pass recovers low single digits; size is not what it would buy. **Agent-boundary modularization is largely already done.** Agent-specific content is 29,030 B (20.6%) but splits at the archive fetch: **18,264 B (13.0%) must stay** in the curl'd file — the Agent Registry, MCP registration, agent selection, permission seeding and the self-update install paths all run in §Pre-flight Phase, which by design completes *before* the archive exists, so a file the archive would supply is not there yet. That leaves 10,766 B (7.6%) extractable, of which the clean part (the Antigravity and OpenCode layout sections) is 5,338 B (3.8%). `framework/bootstrap/configure/{key}.md` — 4 files, 35 KB — is the project having already extracted the post-archive half; the registry's *Configure source path* derived value is that seam. **The archive boundary is the split worth scoping.** Pre-flight 82,705 B (58.7%) / procedure 58,280 B (41.3%). Feasibility was checked against the whole file: exactly **two** executable forward-dependencies exist, both on the pre-flight stale path, which L440 confirms skips the archive fetch — L409 runs §Post-Write Integrity Check (L1035, 1,096 B, whose own opening names "the **Pre-flight Phase** (stale-write path)", so it is documented dual-use) and L408 invokes the keep-placeholders-literal rule (L951, 479 B). **Pulling 1,575 B (1.1%) forward makes pre-flight self-sufficient**; the other sixteen forward references are narrative (ordering notes, deferred output, "described in §X"). Net: the file that is curl'd, installed into every agent, loaded into context at every invocation and byte-compared on every run drops 140,985 → **~84,280 (−40%)**, and the pre-flight "small fetch" becomes genuinely small again — the property L275 says the phase exists to preserve, and which 141 KB has largely eroded. **Blocked on the sibling ordering item** — the boundary must come from execution order, and §Pre-run Migrations sits at line 466 while reading from the fetched archive. **Two further findings from the same read.** (1) Rationale is **17–25%** of the file (17.0% mechanical floor, 24.5% by judgment), concentrated in 108 lines averaging **503 B against the file's 119** — and only half of it is movable: rationale that **constrains execution** must stay, because it stops an agent doing the tempting wrong thing (L408 "would create a second command file", L436 "names nothing they have", L1024 "matches nothing", L395 "a false `stale` on every run", L262 "Never fall through", L224 "not instructions to execute"), while **dated incident narrative** belongs in the spec that decided the fix with a pointer back (L219's 0.29.10-against-0.31.0 adopter, L275's greenfield halt, L1026's 370 shipped placeholders) — roughly 8–15 KB. Stripping rationale wholesale would yield a shorter file that behaves worse. (2) An operator call rather than a cleanup: `framework/bootstrap/govern.md` is **140,985 B of byte-identical duplication** held by audit Family 21, so the bootstrap costs 282 KB in-repo and every new command costs a `cp`; how long that transitional alias lives — and therefore how long unmigrated adopters keep their fetch path — is a decision nobody has made
- [ ] bug: `specs/022-deterministic-runtime/scenarios/framework-list-dedup.md` says it records an open question that it does not record, and two shipped configure files point adopters at that non-existent home. Its **Resolved Questions** entry *Auggie permission format* ends "is a plan-phase decision **recorded as an open question on this scenario**", while the scenario's own `## Open Questions` section reads "*None — captured during scenario authoring.*". Measured 2026-09-13 by following the pointer: nothing under 022 tracks it — no Open Question, no scenario, no `tasks.md` entry, no `spec.md` mention (`grep -n 'antigravity' specs/022-deterministic-runtime/{tasks,spec}.md | grep -i 'permission|merge|format'` is empty). So a deferred design decision claims a durable home, the home is empty, and every gate is green — [§design-principles](../framework/constitution.md#design-principles)'s *work that is not complete MUST never be indistinguishable from work that is*, in the shape where the record itself is the thing missing. **Two shipped files inherit it**, which is what makes it adopter-facing rather than internal: `framework/bootstrap/configure/auggie.md`'s §Instructions note sends the reader to that exact scenario path for the decision's status, and `framework/bootstrap/configure/antigravity.md`'s note makes the vaguer claim "tracked on spec 022" for the same question about a third permission format. Both are `update`-strategy manifest destinations, so every adopter receives the dead pointer. **Verified rather than assumed**: `merge-permissions` really does serve only `permissions.allow`/`permissions.deny` string arrays, so the underlying deferral is real and current — this is about the missing record, not about the primitive. **Why it is logged rather than fixed in the pass that found it** (029's backfill, which had `configure/{auggie,antigravity}.md` in review scope): the principal half is a durable contract under a spec the campaign has **closed**, so standing operator decision (a) on the backfill item binds — editing `framework-list-dedup.md` reopens 022 and, because a scenario is a durable contract, obliges a full five-pass re-review of the corpus's largest spec. Rewording only the two `framework/` notes would be worse than leaving them: it would remove the last trace of a decision that is genuinely still open. **Three shapes on the table**, none decided here: restore the Open Question to `framework-list-dedup.md` (cheapest content, costs the 022 back-edge); move the question to a scenario under whichever spec owns the third permission format and re-point both notes (same back-edge, better home); or resolve the deferral outright by giving `merge-permissions` a format argument, which is a `runtime/` change and therefore a version bump plus a `ductus-v<version>` tag. Route with the other 022-blocked items rather than alone — decision (c)'s lesson is that a batch is one release and five singletons are never. (captured during /ductus:review of 029-bootstrap-runtime-autowire)
- [ ] convention: 62 `§` references outside `spec.md` have never resolved, and the sweep that fixed the `spec.md` tier was recorded as corpus-wide. **Measured 2026-09-13** by running `ductus resolve-anchor --markers-path framework/constitution.md` over every tracked spec artifact at the close of 029's pass: all 54 `spec.md` files report `unresolved: []`, and **62 references outside them do not**. A `§X` reference resolves by LINE — it counts as `qualified` only when the same line names a markdown document other than the markers file — so each of these reads as a claim about the constitution, where the named section does not exist. **The cost split is the whole planning input, because it decides what a fix is worth:** **31 refs across 16 durable-contract files** (`scenarios/*.md` and `data-model.md`, in 10 specs — 003, 013, 022, 023, 027, 028, 032, 045, 048, 050), each of which stales its spec's `reviewed-digest` and therefore obliges a five-pass re-review; and **31 refs across 15 `plan.md` files**, which `write_review.rs` does not digest, so that half reopens nothing, stales no review, and is free. **Four of the ten durable-side specs — 013, 023, 045 and 048 — are still on the backfill campaign's Remaining list**, so their share costs *nothing extra* if swept inside their own pass, exactly as 029 swept its own 14 (plus 2 in its plan) at no marginal cost. That leaves six closed specs (003, 022, 027, 028, 032, 050) whose share is blocked by standing operator decision (a) unless the reopen is deliberately spent — and 022's is the expensive one, per the same reasoning that made its validation fix its own unit. **Recommended shape, not decided here:** do the `plan.md` half as a chore in one commit (free, 15 files, verified by re-running `resolve-anchor` to `unresolved: []`), let the four Remaining specs absorb theirs during their scheduled passes, and leave the six closed specs' share as the only part that needs a routing decision. **The second half of this item is the entry, not the references.** `AGENTS.md` §Gotchas described that sweep as **corpus-wide** when it was `spec.md`-scoped, so a reader checking whether the class was handled would have concluded yes — the "a sweep's own enumeration is itself a claim" failure the same file teaches two entries away, committed by the entry that teaches it. Corrected in place on 2026-09-13 with the measurement above; recorded here because the correction is what makes the 62 visible, and nothing else would have surfaced them. (captured during /ductus:review of 029-bootstrap-runtime-autowire)
- [ ] convention: 022's `check-artifacts` registry has fallen behind shipped behaviour in three measured places, and the runtime's own module doc in a fourth. `specs/022-deterministic-runtime/data-model.md:1011` says *Eight families* where nine ship, and `analyze-state-drift` (added by 047) appears nowhere in that file — 0 occurrences. The same line glosses `review-state-drift` as two conditions where three ship; the third is the non-zero `should-violations` check 045's task 15 added, which this pass documented in `analyze.md`. And `specs/022-deterministic-runtime/scenarios/criterion-path-existence-family.md`'s final Edge Case still says in the present tense that an adopter-layout path whose top-level segment also exists here *still flags*, which `ships_to_adopter` (landed in `6e37efa5`, under 022 itself) changed to a `ships-to-adopter` skip. `runtime/src/primitives/check_artifacts.rs`'s module doc carries the same two-condition `review-state-drift` gloss, and quotes 045's AC18 with the single-commit attribution this pass corrected. Cost, which is what sets the disposition: the first three are one 022 reopen, and the scenario is a durable contract, so they oblige a full five-pass re-review of the corpus's largest spec — standing operator decision (a) keeps that out of this pass, and the campaign item already records a full 022 re-review as its own unit. The fourth is a `runtime/` doc-comment edit and therefore carries a version bump and a `ductus-v<version>` tag. — `specs/022-deterministic-runtime/data-model.md:1011` (captured during review of 045-decision-state-drift-detection)
