# Inbox

<!-- Rules:
     - Do not frontfill bugs that are not being actively worked on.
     - A bug or omission inside the scope of the spec currently in progress does NOT belong
       here — it becomes a task on that spec's tasks.md. The inbox is for findings with no
       home yet; an in-progress spec is already the home, so an item logged here is routed
       straight back to it (constitution §brownfield-inbox, scope decides the destination).
     - Write specs for areas being actively touched — let adoption spread naturally.
     - As specs are written, items migrate from here into spec updates or new scenarios.
     - Chores (project maintenance with no feature home — lint/formatting cleanup,
       dependency cleanup, repo hygiene) also live here; /groom recognizes them and leaves
       them in place. They clear when done, not by migrating to a spec.
     - The brownfield backlog drains toward empty as adoption completes; incidental
       capture is ongoing, so the file persists while work keeps surfacing issues.
     - Status notes do NOT belong here. Every item must be routable by /groom to one of
       its five routes (rule, spec, scenario, chore, discard); a "where things stand"
       or "what to do next" note matches none of them, so it would be walked and
       re-discarded on every pass forever. Pipeline state is derived — read it from
       /status, tasks.md, and git — not narrated into the backlog.

     Format each item as a checkbox list entry with a brief description and any relevant
     context. Three forms are in use:

     1. Manual entry (via /log) — the simple form below:
        `- [ ] {Brief description of the issue and any relevant context}`

     2. Auto-captured finding (an agent recorded this automatically while working a task,
        per §brownfield-inbox Automatic issue capture). Lead with a category so /groom can
        route it, and include a source pointer:
        `- [ ] {category}: {summary} — {file:line or area} (captured during {NNN-feature})`
        Categories: security, leak (memory/resource), convention, bug, perf, other.
        Security issues and leaks are the highest-priority captures.

     3. Audit finding written by /ductus — stricter form (see
        specs/008-security-rules/spec.md): `- [ ] {Rule ID}: {artifact} does not address — {summary}`.

     When an item is migrated, remove it from this list. -->

- [ ] convention: grounding — a `done` spec corpus, two rule files and the runtime record another adopter project's name, against §Workflow's "never record another project's name in this repository — describe the shape instead". Measured 2026-09-13: ~100 occurrences. They split three ways and only the first is a straight sweep. (1) **Spec prose** — `000` (spec + plan, 6 hits), `001` (spec + plan, 3), `002` (spec + plan + tasks, 5), `017` (2 scenarios), `022` (6 scenarios), `036` (spec + plan + tasks, 4): each states *what kind of project* it derived from or was motivated by, so the name drops out with no loss of meaning. (2) **`framework/rules/quality-cross.md`** `QUAL-STUB-001`'s Rationale and `framework/migrations/{ductus-rename,session-file-consolidate}.md` — these **ship to adopters**, which makes the leak worse than in a spec body. (3) **`runtime/`** — source doc comments, `CHANGELOG.md`, and test fixtures that use the name as a *placeholder project* (`tests/fixtures/exec-auggie/`, `exec-opencode/`, `parity.rs`, `host.rs`): a fixture is the sanctioned form per that same entry, so whether these count is an operator call, and changing them is a `runtime/` edit that needs the version bump and `ductus-v<version>` tag. Note the entry's own warning: the replacement text varies per sentence, so this is **not** a uniform substitution and does not earn the mechanical-sweep exemption — each affected spec takes the back-edge (captured during /ductus:analyze of 001). **Tier (2) is DONE (2026-09-13)** — the shipped half, which the item itself called the worse leak: `framework/rules/quality-cross.md` `QUAL-STUB-001`'s Rationale now says *an adopter project running a backend service*, `framework/migrations/ductus-rename.md` says `/{project}:specify`, and `framework/migrations/session-file-consolidate.md`'s two examples use a placeholder `acme`. `git grep -i anvil -- framework/` is now empty, so nothing adopters receive carries the name. **Tiers (1) and (3) remain; tier (2) is done and verified empty.** **Re-measured 2026-09-14 during grooming, because the tier list above had decayed in both directions.** Tier (1) is **27 occurrences across six closed specs**: the first name in `001` (3), `002` (6), `017` (2), `022` (10) and `036` (4), and a **second adopter name** — folded in from a separate item on 2026-09-13 — in `022/scenarios/derive-references-unstaged-drift-is-reported.md` (1) and `046/spec.md` (1). `000`'s and `045`'s shares are **discharged** in their own passes, so the six-spec list this item used to carry named one spec that is clean and missed `046`'s durable-contract hit. **Disposal under the two-disposal rule** (`AGENTS.md` §Workflow): there is no not-a-spec home for prose *inside* a spec, so tier (1) is spend-the-reopen — and **022's eleven ride the 022 batch for free**, leaving `001`, `002`, `017`, `036` and `046` as five deliberate reopens, of which `022`'s and `046`'s hits are the durable-contract kind that cost a five-pass re-review rather than an analyze. Tier (3) is **81 occurrences across 9 `runtime/src/` files and 8 `runtime/tests/` files**; it carries the version bump and the `ductus-v<version>` tag, so it belongs in the 0.49.5 batch — and the **operator call it needs is unchanged and is not groom's to make**: `AGENTS.md` §Workflow sanctions a fixture as the replacement for a real adopter's name, so whether a fixture *using* that name counts as recording it is the question, and it is raised when the release is cut rather than decided here. **The second adopter name has been stripped from this item** (2026-09-14): spelling it out here was the rule's own violation sitting in the file that reports it, and the inbox is not a spec artifact, so removing it reopened nothing and staled no review. Both names are merged into this one item with the full spread rather than carried separately and rediscovered twice.
- [ ] bug: `specs/022-deterministic-runtime/scenarios/unreadable-scenario-is-reported.md` contradicts shipped behaviour on two sentences, and a scenario is a durable requirement document rather than a note. It states **"Nothing gains a block"** and, of the `check-artifacts` skipped record, **"It is not a finding: the file is an unknown, not a defect."** Commit `8c4ca74` (*feat(022): artifact-unreadable blocks a done spec*) reversed the second half: `check_artifacts.rs` now documents and implements *"**artifact-unreadable at `done`** (blocking, across families) — a skipped target below `done` and a **blocking finding at it**"*, and `framework/commands/analyze.md` §Unexamined targets carries the matching exception, naming `scenario-open-questions` as the motivating family. The first half is still exactly right and must not be swept with it: `check_review_gate.rs:803` still yields no block for an unreadable scenario, deliberately, so the gate/finding split is the whole point and a blanket edit would destroy it. Measured 2026-09-13 while re-reviewing 046, whose AC22 carried the same reversed claim and is now annotated. Fix is surgical — two sentences in the scenario's §Behavior, plus its Edge Cases bullet on the `link-adjacent-drift` double-record — but it edits a durable contract under a `done` spec, so it takes 022's back-edge and stales 022's `reviewed-digest`, which is why it is logged rather than folded into 046's pass. **Routed 2026-09-14: spend the reopen, batched into the single 022 unit** — the two-disposal rule in `AGENTS.md` §Workflow (where the campaign item's standing decisions were re-homed when it was retired) leaves no not-a-spec home for a claim stated inside a 022 scenario, and 022's reopen is being spent once for six items rather than six times for one each. Re-verified at HEAD 2026-09-14: line 21 still reads *not a finding* against `check_artifacts.rs`'s module doc making artifact-unreadable blocking at `done`, and line 23's *Nothing gains a block* is still correct, so the surgical two-sentence scope holds (captured during /ductus:analyze of 046)
- [ ] convention: .claude/commands/ductus/init.md is an orphan that no sweep reaches, and it has drifted across five specs' renames. It is the one hand-maintained file under .claude/ (AGENTS.md §Gotchas) — no framework/commands/init.md source, so no generator rewrites it; and .claude/ is deliberately absent from AGENTS.md's sweep-target manifest, correctly, because every other file there is generated output. init is also absent from help.md, README.md, docs/slash-commands.md and scripts/maintainer-only-commands.txt (which lists only `audit`), and Family 16 anchors on framework/commands/*.md source files so it never sees init either. Measured 2026-09-13, five drifts: (1) it copies 6 of the 11 shipped rule files — concurrency-backend, observability-backend, performance-backend, quality-cross and reliability-backend are missing, so a scaffolded project silently gets a smaller rule set than /ductus installs; (2) it copies a spec-and-plan.md template that 023 deleted; (3) it writes the constitution to repo-root constitution.md, the pre-044 location, not .ductus/constitution.md; (4) it writes a new project's config to .govern.toml at two sites — a write to a fresh tree, not a read of a legacy tier, so it scaffolds straight into the layout the migrations exist to undo; (5) it creates .claude/{slug}-session.json, while the session file is .ductus/session.toml for every adopter, project-name-agnostic and TOML. It also never acquires or wires the runtime, which §runtime-boundary principle 3 now makes required. This is the drift §drift-prevention's Manifest discipline exists to prevent, and that rule names this exact pair as its example — "/ductus and /{project}:init both scaffold a project" — so the framework constitution ships adopters a reference to a command they never receive. Repair-vs-retire is an operator call, not a sweep: /ductus supersedes init for every adopter, and init's only remaining role is scaffolding a brand-new project locally. None of this falsifies a 004 criterion — all nine hold. — `.claude/commands/ductus/init.md` (captured during review of 004-tech-stack-selection). **Operator decision 2026-09-13: retire it.** `/ductus` already covers the greenfield case — `framework/bootstrap/ductus.md` names "greenfield run" and "a fresh project where none exists", needing only `git init` first — so init's one remaining role is already served and repairing five drifts would preserve an orphan that drifts again on the next rename. Delete the file and update the constitution's §drift-prevention Manifest-discipline example, which is a 050 back-edge. **The decision stands; its price does not — re-measured 2026-09-14 during grooming, the "only live reference" clause is false and it is the clause the whole cost estimate rested on.** Deleting the file also falsifies **three ticked acceptance criteria** and a present-tense claim, each in a spec the corpus has closed: `003`'s **AC5** (*Command exists at `.claude/commands/ductus/init.md`*) goes outright false, `040`'s **AC6** (*`/ductus:init` scaffolds the spec-root directory … under the configured name*) asserts behaviour of a command that no longer exists, `026`'s **AC4** and its §Behavior §2 make the `/ductus` vs `/ductus:init` comparison the entire subject of audit Family 2 — AC4 already annotates the installer half as never built, so the annotation grows rather than the claim changing — and `043`/spec.md:49 states in the present tense that the hand-maintained copy *is swept by hand*. `scripts/audit/manifest-parity.sh`'s header names it too, but that is `scripts/` and free. **So this is not a file deletion plus one constitution line; it is a deletion touching four closed specs' bodies, three of them through ticked criteria, and it is its own unit rather than a grooming chore.** Disposal under the two-disposal rule (`AGENTS.md` §Workflow): spend the reopens — there is no not-a-spec home for a criterion that names the deleted file. `026` is the cheap one to sequence, since it is already the spec whose Family 2 the deletion changes; the other three are `spec.md`-only edits, so each costs a reopen plus an analyze and **none stales a `reviewed-digest`**, which is what makes the whole unit affordable in one sitting
- [ ] bug: a non-zero `analyze.advisory` count with no corresponding inbox capture is undetectable. `analyze.md` step 16 is what persists a finding's *content* — an `append-inbox` call per surviving finding, before the render — while `write-analysis` records only the *count*, and the constitution's §brownfield-inbox says both capture paths MUST be captured rather than dropped. Nothing holds the two against each other. The review half has `check-review-agreement` (Family 31) pinning `review.md` to its frontmatter; analyze has no counterpart, because it writes no report artifact for one to compare against — so a run that records `advisory: N` and appends nothing is byte-identical to one that captured all N, which is `QUAL-CLAIM-001` turned on the analyze record itself. **Measured 2026-09-13.** Every per-spec run checks out: the four advisories recorded on 2026-09-13 are all accounted for — 001, 011 and 046 each have a live inbox item carrying its `(captured during /ductus:analyze of NNN)` marker, and 002's was correctly *not* re-appended because it was the adopter-project-name class already filed under 001, which is what the `dedup-prefix` guard exists to do. The one divergence is a **batch** run: `7963f134` (2026-09-06, "drain the analyze-record backlog to zero") wrote 50 analyze records totalling `advisory 21` and touched `specs/inbox.md` not at all — confirmed by `git show --stat` on it and on its sibling `0405f6f7`. That instance was benign and deliberate rather than a slip: its commit message identifies the findings as one class (prose-qualified `§` references to the installer's sections written without naming `ductus.md` on the line, plus 023's two historical anchors), records the decision to carry rather than chase them across six back-edge cycles for findings that by construction do not block, and the class later got a durable home in `AGENTS.md` §Gotchas naming 023, 029, 031 and 032. So this is not a lost-content report — it is that the same skip performed carelessly would look exactly the same. Ten of those advisories still stand in frontmatter today (032:3, 031:3, 029:2, 023:2). Routing is `/groom`'s call, not this item's; the shapes on the table are an analyze-side agreement check (the `check-review-agreement` counterpart, which needs something durable to compare against), or making the recorded count derivable from the captures rather than supplied alongside them. Found during a post-close survey of work tracked outside the inbox, not during an analyze run — so no capture marker applies. **Routed 2026-09-14: 022 reopen plus the 0.49.5 release, paired with the false-empty-scope item below.** The two are one defect class — a pipeline record that reads identically whether the work behind it happened or not, which is `QUAL-CLAIM-001` turned on the framework's own artifacts — and pairing them means one tag rather than two. The shape chosen of the two this item offered is **deriving the recorded count from the captures rather than accepting it alongside them**, because it needs no new check and no new durable artifact to compare against: `write-analysis` already refuses to record a clean analyze without `unexamined`, for exactly this reason, so this is that guarantee extended to the field beside it rather than a new mechanism. Discard was weighed and declined — the item measures **zero** instances of lost content, but the bar is *would you stop and fix it today*, and a release is already being cut for its twin
- [ ] convention: second promotion round — audit `AGENTS.md` against `framework/constitution.md` and move everything adopter-beneficial into the constitution, consolidating anything stated in both to the constitution with an `AGENTS.md` link. **Operator request 2026-09-13**, stated as two halves: (a) everything currently in `AGENTS.md` that can benefit adopters moves to `framework/constitution.md`, which is the one artifact adopters receive; (b) any instruction living in *both* is consolidated into the constitution, with `AGENTS.md` keeping a link. **Measured 2026-09-13.** `AGENTS.md` is 148 lines / 135,952 bytes carrying 103 top-level bullets: 92 rule-bearing across the four sections 050's plan scopes (Workflow 44, Gotchas 40, Boundaries 3, Design Principles 5) plus 11 descriptive (Project Structure 6, Tech Stack 5) that 050 correctly held out of scope. **41 entries cite a `framework/constitution.md#` anchor; 62 cite none.** Of the 41, only 31 use the established mirror phrasing (26 "contributor-side mirror", 5 "contributor-side record"), so roughly 10 cite the constitution without declaring themselves mirrors — which is exactly where half (b)'s duplication hides, since a restatement that happens to link is indistinguishable from a pointer by grep. The constitution is 760 lines across 32 anchored sections. **This is 050's territory and needs no new method.** §drift-prevention's canonical-sources map names `specs/050-constitution/spec.md` as the home for constitution-content work — "what belongs in it, how it is organized, which rules adopters receive" — and 050 already supplies every instrument this needs: the **reword test** (promote iff the rule restates without naming machinery only this repo has *and* without losing what makes it actionable), the three-tier **Classification** (universal → constitution; shared → an org's registered `[constitutions.*]` document per 055; project-only → stays), and **§Promotion mechanism** (one normative statement in the constitution, one `AGENTS.md` line that points and states nothing of its own — which is half (b) exactly). 050's plan already anticipates this run: "the table is a snapshot of one pass, and a later promotion round re-derives rather than amends it." **Root cause — do the flow fix first, before the 92-entry pass.** The backlog is not neglect and 050's AC1 did not cause it: AC1 correctly rejected a per-entry marker, since a field an author must remember to fill is the diligence dependency §design-principles rejects. The cause is that **every write-time routing rule discriminates by *kind*, while promotion discriminates by *population*.** Three sites tell an author where a learning goes and none asks the universality question: `AGENTS.md:5`'s header says only "append durable learnings to the matching section"; `CLAUDE.md`'s Auto-Memory Routing asks "would this help any other contributor **to this project**?"; and the constitution's §drift-prevention *Shared knowledge stays in git* answers by kind — *"A **project learning** — a convention, a gotcha, a workflow rule, a boundary — goes in `AGENTS.md`."* So a **universal** gotcha lands in `AGENTS.md` **because it is a gotcha**, not because anyone judged it project-only, and the population test that would catch it (050 §Classification: "one question about the rule's population") lives in a spec nobody reads while writing one. The default manufactures the backlog continuously — 54 rule-bearing entries at the survey, **92** now — so running the 92-entry pass without changing the routing just resets a counter that refills. **The fix is not a new authored input**, which is why §design-principles does not bar it: an author already chooses a destination, and changing *which question decides it* adds no remembered step. Move 050's population question into §drift-prevention's *Shared knowledge stays in git* bullets, and mirror it at `AGENTS.md:5` and in `CLAUDE.md`'s routing block — three small edits plus one 050 back-edge, landing **before** the bulk pass. **Correction to the counts above, which are proxies rather than measures.** Neither grep is the answer: the 62 non-citing entries **overcount** the backlog because 28 are settled project-only from 050's pass, and the 41 citing entries **overcount** the promoted set because project-only entries cite `§spec-lifecycle` and friends for context — 41 against the 26 that pass actually produced. A sound coverage check needs 050's classification table as an input, **re-keyed from positional IDs (`W4`, `G11`) to each entry's bolded lead phrase**, since positional IDs drift the moment an entry is inserted and the table is already stale by that mechanism alone. Then *unclassified = rule-bearing − (table-keyed ∪ constitution-citing)*, a new entry is unclassified **by default** (the honest state, and the opposite of a field that silently stays empty), and the number today is **~34**, not 62. Render it as a standing notice on the §brownfield-inbox inbox-row precedent — **notice, never gate**, because promotion has to stay free or the honest choice between a growing backlog and a silent one pushes toward silence. The re-keying is work this round must do anyway. **Honest limit on both halves:** neither makes promotion happen; one stops new misrouting and the other makes not-having-promoted visible. The reword test stays human judgment, which §runtime-boundary principle 2 keeps off the runtime regardless. **Scope note for whoever takes it:** this is a spec cycle of its own, not a session tail — 92 entries judged individually, each promotion reworded for adopter-neutrality with its reason recorded (AC1's audit trail), and every promoted entry's `AGENTS.md` line rewritten to a pointer. Expect the constitution to grow materially, so §Resolved Questions' bullets-not-sections rule and AC10's anchor-stability constraint both bind. Two constraints already settled and not to be re-litigated: a constitution-only change moves no version pin and needs no tag (050 §Resolved Questions), and the mirror stays in `AGENTS.md` rather than being deleted, because contributors read it as the index of how to work here and a rule that silently left reads as a rule that was dropped. **Routed 2026-09-14: a 050 back-edge, in two units and in the order this item already specifies — the root-cause flow fix first, then the bulk pass as its own spec cycle rather than a session tail.** **Every count above is superseded; re-measured the same day, and the decay is this item's own thesis borne out at the resolution of hours.** `AGENTS.md` is now **164 lines / 204,757 bytes** carrying **117** top-level bullets — **106 rule-bearing** across the four in-scope sections (Workflow 48, Gotchas 48, Boundaries 3, Design Principles 7) plus the same 11 descriptive ones 050 held out of scope — with **47** citing a `framework/constitution.md#` anchor and **32** using the declared-mirror phrasing. Against the 148 lines / 135,952 bytes / 103 bullets / **92** rule-bearing / 41 citing / 31 mirrors recorded above, the backlog grew **92 → 106 in a single day** and the file grew ~69KB. That is the item's own root-cause claim demonstrated rather than argued: the write-time default routes by *kind* and refills the queue faster than any bulk pass can drain it, so **running the bulk pass without the flow fix first resets a counter that refills**, and the ordering is not a preference. The corollary for whoever takes it: **do not promote entries opportunistically** while doing other work — a partial pass leaves the unclassified set unmeasurable, which is the state the standing-notice half exists to end. Re-derive the counts again at the start of the pass; the ones in this paragraph are evidence about 2026-09-14 and nothing more
- [ ] convention: `framework/bootstrap/ductus.md` is 141 KB and could be split at the **archive boundary** for a ~40% cut to what adopters install — but not at the agent boundary, which is mostly already done. **Measured 2026-09-13, whole file read (1182 lines / 140,985 bytes).** Composition: **90.2% prose**, 7.0% tables, 2.8% fenced blocks, so the cheap theories all fail on measurement — the shell fallback assumed to duplicate the deterministic path is **2.8%**, the registry's four inline JSON permission blobs are **2.5%**, and retired-release tokens (`.govern/`, `.govern.toml`, `gvrn`, "retired", "pre-04x") total **14 lines** whose config-ladder tiers are load-bearing per §Gotchas. A dead-code pass recovers low single digits; size is not what it would buy. **Agent-boundary modularization is largely already done.** Agent-specific content is 29,030 B (20.6%) but splits at the archive fetch: **18,264 B (13.0%) must stay** in the curl'd file — the Agent Registry, MCP registration, agent selection, permission seeding and the self-update install paths all run in §Pre-flight Phase, which by design completes *before* the archive exists, so a file the archive would supply is not there yet. That leaves 10,766 B (7.6%) extractable, of which the clean part (the Antigravity and OpenCode layout sections) is 5,338 B (3.8%). `framework/bootstrap/configure/{key}.md` — 4 files, 35 KB — is the project having already extracted the post-archive half; the registry's *Configure source path* derived value is that seam. **The archive boundary is the split worth scoping.** Pre-flight 82,705 B (58.7%) / procedure 58,280 B (41.3%). Feasibility was checked against the whole file: exactly **two** executable forward-dependencies exist, both on the pre-flight stale path, which L440 confirms skips the archive fetch — L409 runs §Post-Write Integrity Check (L1035, 1,096 B, whose own opening names "the **Pre-flight Phase** (stale-write path)", so it is documented dual-use) and L408 invokes the keep-placeholders-literal rule (L951, 479 B). **Pulling 1,575 B (1.1%) forward makes pre-flight self-sufficient**; the other sixteen forward references are narrative (ordering notes, deferred output, "described in §X"). Net: the file that is curl'd, installed into every agent, loaded into context at every invocation and byte-compared on every run drops 140,985 → **~84,280 (−40%)**, and the pre-flight "small fetch" becomes genuinely small again — the property L275 says the phase exists to preserve, and which 141 KB has largely eroded. **No longer blocked — the sibling ordering item it waited on was resolved 2026-09-13 and nothing said so.** The blocker was real when written: the boundary has to come from execution order, and §Pre-run Migrations sits at line 466 while reading from the fetched archive, so the skipped-section list read as a sequence put migrations ahead of the fetch. `86a4432b` fixed it as a chore in the grooming pass four hours later — `ductus.md` now says in as many words that the list *names which sections are skipped, not the order they run in*, and that the execution order is the numbered walker in §Instructions, which fetches at step 2 and extracts at step 3, with §Pre-run Migrations necessarily after extraction. So the ordering this split needs is settled and stated. Verified 2026-09-14 by reading the commit and the current file. **The generalisation is why this is written out rather than just deleted:** an item that records a blocker holds a claim about another item, and when that other item is retired nothing revisits the first — every grooming pass since has walked this one, read *blocked*, and re-deferred it. That is the failure [§brownfield-inbox](../framework/constitution.md#brownfield-inbox)'s no-hold-state rule names, arriving through an *internal* dependency rather than an external event, which is the case the rule does not cover. When a groom pass retires an item, grep the inbox for anything that named it. **Two further findings from the same read.** (1) Rationale is **17–25%** of the file (17.0% mechanical floor, 24.5% by judgment), concentrated in 108 lines averaging **503 B against the file's 119** — and only half of it is movable: rationale that **constrains execution** must stay, because it stops an agent doing the tempting wrong thing (L408 "would create a second command file", L436 "names nothing they have", L1024 "matches nothing", L395 "a false `stale` on every run", L262 "Never fall through", L224 "not instructions to execute"), while **dated incident narrative** belongs in the spec that decided the fix with a pointer back (L219's 0.29.10-against-0.31.0 adopter, L275's greenfield halt, L1026's 370 shipped placeholders) — roughly 8–15 KB. Stripping rationale wholesale would yield a shorter file that behaves worse. (2) An operator call rather than a cleanup: `framework/bootstrap/govern.md` is **140,985 B of byte-identical duplication** held by audit Family 21, so the bootstrap costs 282 KB in-repo and every new command costs a `cp`; how long that transitional alias lives — and therefore how long unmigrated adopters keep their fetch path — is a decision nobody has made. **Routed 2026-09-14: its own spec cycle, and it is the only large unit in this queue that costs no back-edge at all** — the split touches `framework/` alone, so it reopens no spec and stales no `reviewed-digest`, and the `cp` to `framework/bootstrap/govern.md` rides in the same commit because Family 21 holds the two byte-identical. It is still a spec cycle rather than a chore: it restructures the file every adopter curls, installs into every agent and byte-compares on every run. **Re-measure before scoping — every proportional figure above is derived from a byte count that has already moved.** The file is **146,953 bytes** as of 2026-09-14, not the 140,985 measured above, so the 58.7/41.3 pre-flight/procedure split, the 1,575 B of forward-dependencies to pull forward, and the headline ~40% cut are each a ratio taken against a stale denominator. The structural findings do **not** decay the same way and are what the re-measurement should confirm rather than rediscover: exactly two executable forward-dependencies on the pre-flight stale path, sixteen further references that are narrative only, and the distinction between rationale that **constrains execution** (stays) and dated incident narrative (moves to the spec that decided the fix, with a pointer back)
- [ ] bug: `specs/022-deterministic-runtime/scenarios/framework-list-dedup.md` says it records an open question that it does not record, and two shipped configure files point adopters at that non-existent home. Its **Resolved Questions** entry *Auggie permission format* ends "is a plan-phase decision **recorded as an open question on this scenario**", while the scenario's own `## Open Questions` section reads "*None — captured during scenario authoring.*". Measured 2026-09-13 by following the pointer: nothing under 022 tracks it — no Open Question, no scenario, no `tasks.md` entry, no `spec.md` mention (`grep -n 'antigravity' specs/022-deterministic-runtime/{tasks,spec}.md | grep -i 'permission|merge|format'` is empty). So a deferred design decision claims a durable home, the home is empty, and every gate is green — [§design-principles](../framework/constitution.md#design-principles)'s *work that is not complete MUST never be indistinguishable from work that is*, in the shape where the record itself is the thing missing. **Two shipped files inherit it**, which is what makes it adopter-facing rather than internal: `framework/bootstrap/configure/auggie.md`'s §Instructions note sends the reader to that exact scenario path for the decision's status, and `framework/bootstrap/configure/antigravity.md`'s note makes the vaguer claim "tracked on spec 022" for the same question about a third permission format. Both are `update`-strategy manifest destinations, so every adopter receives the dead pointer. **Verified rather than assumed**: `merge-permissions` really does serve only `permissions.allow`/`permissions.deny` string arrays, so the underlying deferral is real and current — this is about the missing record, not about the primitive. **Why it is logged rather than fixed in the pass that found it** (029's backfill, which had `configure/{auggie,antigravity}.md` in review scope): the principal half is a durable contract under a spec the campaign has **closed**, so the two-disposal rule in `AGENTS.md` §Workflow binds — editing `framework-list-dedup.md` reopens 022 and, because a scenario is a durable contract, obliges a full five-pass re-review of the corpus's largest spec. Rewording only the two `framework/` notes would be worse than leaving them: it would remove the last trace of a decision that is genuinely still open. **Three shapes on the table**, none decided here: restore the Open Question to `framework-list-dedup.md` (cheapest content, costs the 022 back-edge); move the question to a scenario under whichever spec owns the third permission format and re-point both notes (same back-edge, better home); or resolve the deferral outright by giving `merge-permissions` a format argument, which is a `runtime/` change and therefore a version bump plus a `ductus-v<version>` tag. **Routed 2026-09-14: spend the 022 reopen, batched, and prefer the third shape.** This is the item the loss of decision (a)'s first disposal hits hardest — it was parked waiting for 022's own later pass, the campaign closed at 54 of 54 with no such pass scheduled, and the two-disposal rule now in `AGENTS.md` §Workflow leaves only *spend the reopen* or *route it off the spec*. Of the three shapes recorded above, the third — give `merge-permissions` a format argument — is the one to take: it is a `runtime/` change, so it lands in the same 0.49.5 batch the other runtime items need, and it **resolves** the deferral rather than re-recording it, after which the two shipped `configure/{auggie,antigravity}.md` notes point at a decision that was made instead of at a home that is empty. The first shape (restore the Open Question to the scenario) stays available as the fallback if the format work proves larger than it looks; what is ruled out is the option this item already ruled out, rewording the two `framework/` notes alone, which would delete the last trace of a decision that is genuinely still open. (captured during /ductus:review of 029-bootstrap-runtime-autowire)
- [ ] convention: **120 `§` reference occurrences (95 distinct anchor names) outside `spec.md` have never resolved — re-measured 2026-09-14, against the 62 this item opened with,** and the sweep that fixed the `spec.md` tier was recorded as corpus-wide. **Measured 2026-09-13** by running `ductus resolve-anchor --markers-path framework/constitution.md` over every tracked spec artifact at the close of 029's pass: all 54 `spec.md` files report `unresolved: []`, and **62 references outside them do not**. A `§X` reference resolves by LINE — it counts as `qualified` only when the same line names a markdown document other than the markers file — so each of these reads as a claim about the constitution, where the named section does not exist. **The 62 was wrong twice and the correction is the item's most useful half.** It counted only two destinations — `tasks.md` and `review.md` had never been scanned — and it read `resolve-anchor`'s `unresolved` field, which is a **deduplicated set of anchor names** rather than a list of occurrences, so it understated the repair even within its own scope. Filter `references` on `resolved: false` for the real list; 048's pass was handed "8 unresolved" and found **12** lines, because one file named `§Runtime acquisition` four times. **The cost split by destination, re-measured 2026-09-14, is the whole planning input:** **19 occurrences across 11 durable-contract files** (`scenarios/*.md` and `data-model.md`) — the only tier that stales a `reviewed-digest` and therefore obliges a five-pass re-review; **28 across 13 `plan.md` files**, which `write_review.rs` does not digest, so that half reopens nothing and is free; **57 across 17 `tasks.md` files**; and **16 across 12 `review.md` files**. **The last two are close to worthless to repair and should probably be excluded outright**: `tasks.md` is ephemeral and `/{project}:prune`-able by construction, and `review.md` is regenerated wholesale by the next `write-review`, so a reference fixed there is destroyed the next time that spec is reviewed — which, for any spec the backfill campaign still touches, is soon. Deciding that is a routing call, not this item's to make; what the item now carries is the measurement that lets someone make it. **All four durable-side specs that the `examined`/`scope` backfill campaign still had open when this was written have since discharged their share inside their own passes** — 045 (one, `§Behavior` in its `data-model.md`), 013 (three), 023 (two, with two in `tasks.md` deliberately left) and 048 (twelve occurrences across four files, 2026-09-14) — exactly as 029 swept its own 14 plus 2 at no marginal cost. **026's one — `§Future` in its `plan.md` — is discharged** (2026-09-14, in its own backfill pass; the pointer named a `## Future Considerations` section removed when Family 9 was adopted, so the repair was to correct the claim rather than to make the anchor resolve). No spec on the durable tier is now waiting on a scheduled pass, because the campaign has none left. Nothing else on the durable tier belongs to a spec the campaign has yet to open. That leaves six closed specs (003, 022, 027, 028, 032, 050) whose share needs a deliberate reopen under the two-disposal rule in `AGENTS.md` §Workflow — and 022's is the expensive one, per the same reasoning that made its validation fix its own unit. **Routed and partly discharged 2026-09-14; all four tiers re-measured rather than quoted, and the total is 119 occurrences, not 120.** **(1) The `plan.md` tier is DONE** — it measured **27 across 12 files**, of which **22 were pure qualification repairs** (name the owning document on the reference's own line) and were applied and verified in the grooming pass, taking `plan.md` from 27 unresolved to **5**. Those 22 are mechanical under [§spec-lifecycle](../framework/constitution.md#spec-lifecycle) — the claim does not move, only the line names the document it always meant — so **no spec reopened and no `reviewed-digest` staled**. **The 5 left are named with reasons rather than left silent, because three of them are not qualification repairs at all**: `024/plan.md:20`, `025/plan.md:83` and `033/plan.md:37` each cite a **`§Behavior`** section, and no `framework/commands/*.md` file carries one any more (verified — the commands were restructured into `## Instructions` plus a `## Markdown-only reference`), so the target is *gone* and the repair is a factual **correction of the claim**, which takes the back-edge on each of those three specs; `027/plan.md:21` sits inside a fenced TOML block as illustrative code; and `043/plan.md:33` is a **quotation of text the spec deleted**, so making it resolve would destroy the record it exists to hold. **(2) The `tasks.md` (57 across 17) and `review.md` (16 across 12) tiers are DISCARDED**, which this item's own analysis already argued for and left to a routing call: `tasks.md` is ephemeral and `/{project}:prune`-able by construction, and `review.md` is regenerated wholesale by the next `write-review`, so a reference fixed there is destroyed by the next review of that spec. Neither passes the bar — *would you stop and fix it today* — and [§brownfield-inbox](../framework/constitution.md#brownfield-inbox) has no hold state, so they are discarded rather than carried. **(3) The durable tier is the only part that survives as work: 19 occurrences across 11 files in the six closed specs (003, 022, 027, 028, 032, 050), and 022 holds 11 of the 19 across six of its scenarios** — so they ride the 022 batch for free, inside a pass that re-reviews those contracts anyway, and the remaining 8 across 003, 027, 028, 032 and 050 take the two-disposal rule in `AGENTS.md` §Workflow. **The second half of this item is the entry, not the references.** `AGENTS.md` §Gotchas described that sweep as **corpus-wide** when it was `spec.md`-scoped, so a reader checking whether the class was handled would have concluded yes — the "a sweep's own enumeration is itself a claim" failure the same file teaches two entries away, committed by the entry that teaches it. Corrected in place on 2026-09-13 with the measurement above; recorded here because the correction is what makes the 62 visible, and nothing else would have surfaced them. (captured during /ductus:review of 029-bootstrap-runtime-autowire)
- [ ] convention: 022's `check-artifacts` registry has fallen behind shipped behaviour in three measured places, and the runtime's own module doc in a fourth. `specs/022-deterministic-runtime/data-model.md:1011` says *Eight families* where nine ship, and `analyze-state-drift` (added by 047) appears nowhere in that file — 0 occurrences. The same line glosses `review-state-drift` as two conditions where three ship; the third is the non-zero `should-violations` check 045's task 15 added, which this pass documented in `analyze.md`. And `specs/022-deterministic-runtime/scenarios/criterion-path-existence-family.md`'s final Edge Case still says in the present tense that an adopter-layout path whose top-level segment also exists here *still flags*, which `ships_to_adopter` (landed in `6e37efa5`, under 022 itself) changed to a `ships-to-adopter` skip. `runtime/src/primitives/check_artifacts.rs`'s module doc carries the same two-condition `review-state-drift` gloss, and quotes 045's AC18 with the single-commit attribution this pass corrected. Cost, which is what sets the disposition: the first three are one 022 reopen, and the scenario is a durable contract, so they oblige a full five-pass re-review of the corpus's largest spec — the two-disposal rule in `AGENTS.md` §Workflow keeps that out of a grooming pass, and a full 022 re-review remains its own unit — 022's record reads `examined: 12` of `scope: 87`, which is a truthful small numerator from a digest repair, not a completed pass over its 95 scenarios. The fourth is a `runtime/` doc-comment edit and therefore carries a version bump and a `ductus-v<version>` tag. **Routed 2026-09-14: spend the 022 reopen, batched into the single 022 unit, and ship the `runtime/` half in the 0.49.5 release.** One reopen pays one five-pass re-review for six items rather than six, and one tag pays for five runtime items rather than five tags — the two-disposal rule and its batching arithmetic are in `AGENTS.md` §Workflow, where the retired campaign item's standing decisions were re-homed. Re-verified at HEAD 2026-09-14: `data-model.md:1011` still opens *Eight families* and `analyze-state-drift` occurs **0 times** in that file, so both halves of the headline hold. The first three sites ride the reopen; the fourth, `check_artifacts.rs`'s module doc, is the `runtime/` half and rides the tag — this item is one of the four that is both, so batching costs it nothing extra. — `specs/022-deterministic-runtime/data-model.md:1011` (captured during review of 045-decision-state-drift-detection)
- [ ] bug: a **false empty-scope review record is undetectable by every gate**, so an over-cap `compute-review-scope` misread as "nothing in scope" writes a clean-looking review nothing can catch. **Two code sites, both verified 2026-09-14.** (1) `runtime/src/primitives/write_review.rs:238-248` — `resolve_scope_size` opens with `if args.empty_scope { return 0; }`, so the empty-scope path records `scope: 0` without deriving anything; and its fallback is `map_or(0, …)`, so a derivation that *errors* also records `0`. The doc comment reasons only about a spec dir with no commits, where zero genuinely is coherent; that reasoning does not extend to an errored derivation or to a wrongly-passed flag. (2) `runtime/src/primitives/check_review_agreement.rs:425-458` — both `examined` arms guard on `total > 0` (`(Some(0), Some(total)) if total > 0` → `examined-nothing`; `(None, Some(total)) if total > 0` → `examined-unstated`) and the match ends `_ => {}`, so **`scope: 0` is unreachable by the check at any `examined` value**. Net: the record reads `0/0/0`, `scope: 0`, `examined` absent, `blocking: false` — self-consistent, and invisible to Family 31, to Family 19 (the digest is valid), and to `check-review-gate` (the review is non-blocking). That is `QUAL-CLAIM-001`'s shape inside the artifact the backfill campaign exists to make trustworthy, arriving through the one door `examined`/`scope` do not watch. **Exposure, measured not assumed:** an over-cap MCP call returns a hard `Error: result (120,220 characters) exceeds maximum allowed tokens` plus a saved-output path and no content — it cannot be mistaken for content, only for an *empty scope*; **8 of 54** specs' `compute-review-scope` payloads exceed 100KB (largest 159,303 B for `015`), `diff-cross-spec` for `026` is 99,275 B, and **every other primitive's result sits under 25KB** (`dashboard` 24,909, `check-corpus-links` 24,275, `derive-dependencies` 157), so two git-history-width primitives carry the whole class. **Candidate fix, smallest first:** drop the `empty_scope` short-circuit so the real denominator is always derived — a false empty-scope record would then carry `scope: N` and Family 31 fires `examined-unstated` with no new check written. **Cost is process, not code:** any `runtime/` edit routes to `022-deterministic-runtime` as a scenario per `AGENTS.md` §Workflow, reopening the corpus's largest spec (95 scenarios, 7,521 lines, 113-file review scope) and carrying a version bump plus a `ductus-v<version>` tag in the same sitting — which is why it was deferred rather than done. **Already landed, do not redo:** `framework/commands/review.md` step 1 and its markdown-only reference now state that a failed call is not an empty scope (`0bdc4f7d`), and `AGENTS.md` §Gotchas was corrected — it had described the **Bash** tool's 2KB-preview-plus-pointer behaviour as though it were MCP's, which errors with no preview at all (`e9b76da2`). **Deliberately not taken, with reasons:** `implement.md` steps 7 and 13 carry the same class for `diff-cross-spec`, but informational and non-gating with no durable false record, and step 13 already separates a `guidance` result from an empty one; a `§runtime-boundary` rule that a seam-crossing primitive whose result scales with history must offer a bounded projection would reopen `050-constitution` (8 scenarios, 35-file scope); and a bounded projection on the two primitives is ergonomics the CLI-plus-`jq` route already covers. The repo already practises the unwritten rule three times — `fetch-archive` caps at `MAX_FETCH_BYTES` and errors rather than truncating, `prune-tasks` returns a summary and never the file body, `write-review` truncates descriptions to a budget with a visible ellipsis. **Routed 2026-09-14: spend the 022 reopen, batched into the single 022 unit, and ship the `runtime/` half in the 0.49.5 release.** One reopen pays one five-pass re-review for six items rather than six, and one tag pays for five runtime items rather than five tags — the two-disposal rule and its batching arithmetic are in `AGENTS.md` §Workflow, where the retired campaign item's standing decisions were re-homed. Re-verified at HEAD 2026-09-14: `resolve_scope_size` still opens `if args.empty_scope { return 0; }` and still falls back through `map_or(0, …)`, so an **errored** derivation and a wrongly-passed flag both record `scope: 0`. Take the item's own smallest-first fix — drop the short-circuit so the real denominator is always derived, after which Family 31 fires `examined-unstated` with no new check written. **Paired with the `analyze.advisory` item above**: same defect class (a record that reads identically whether the work behind it happened or not) in sibling primitives, so one tag covers both (captured during /ductus:review of 041-task-pruning)
- [ ] bug: the two frontmatter generators each carry their own sibling-link grammar at exactly three digits, so a body link to a branch-scoped or four-digit spec yields no dependencies: edge. `derive_dependencies::leading_slug` tests `bytes[..3].iter().all(is_ascii_digit) && bytes[3] == b'-'`, and `derive_references` (`:280-289`) carries the same test as an independent copy. `parse_feature_dir` — the single membership rule spec 051 established — accepts both directory forms and three-or-more digits, so the two disagree with it in the same two ways. Proven by probe 2026-09-14 in a scratch repo at /tmp: a spec whose body linked `../1234.1-staged/spec.md` and `../1000-thousandth/spec.md` derived `dependencies: []` while links in the other direction harvested normally; `derive-dependencies` reported `examined: 3`, so the enumeration is right and only the target grammar is wrong. Effects: a dependency on a branch-scoped spec is silently absent, `traverse-deps` inherits the gap through the frontmatter it reads, and `scan_line` is `pub(crate)` for the pre-`done` cross-spec-impact back-link matcher, so a declared impact on such a spec could never be discharged. Not reachable in this corpus, which holds no branch-scoped and no four-digit directory. This falsifies 051's AC5 (`none of them treats a branch-scoped directory as a non-spec`) and AC15 (`no consumer that can call it carries a copy`), both of which were annotated in `a31a7ee2` rather than left standing. Cost, which is why it is recorded and not fixed here: it is a change to a shared parser under `runtime/src/`, so per AGENTS.md §Workflow it routes to `022-deterministic-runtime` as a scenario — reopening the corpus's largest spec and obliging a full five-pass re-review — and carries a version bump plus a `ductus-v<version>` tag in the same sitting. Route it with the other 022-blocked items rather than alone; the batching arithmetic is in `AGENTS.md` §Workflow — a batch is one release and singletons are never. Smallest fix: have both call the shared grammar instead of re-deriving it, which is also what AC15 asks for. **A third copy existed and is discharged (2026-09-14):** `scripts/audit/sibling-coupling.sh`'s `extract_sibling_links` carried the same three-digit grammar, so Family 7 could not see a body link to a branch-scoped sibling and exited 0. Found by 026's review pass over its own scope and fixed there under `QUAL-GROUND-001` — it is shell, so it reopened nothing and needed no release, which is the whole difference between it and the two above. **The fix removed the grammar rather than widening it**, and that is the shape to copy here if it fits: the caller already matched each candidate exactly against a slug the runtime enumerated, so extracting the first segment of every `../SEGMENT/` link is strictly safer than a second copy of the predicate and cannot over-match. Whether `derive_dependencies` and `derive_references` can be restructured the same way — rather than calling the shared grammar — is worth asking before writing the call. **Routed 2026-09-14: spend the 022 reopen, batched into the single 022 unit, and ship the `runtime/` half in the 0.49.5 release.** One reopen pays one five-pass re-review for six items rather than six, and one tag pays for five runtime items rather than five tags — the two-disposal rule and its batching arithmetic are in `AGENTS.md` §Workflow, where the retired campaign item's standing decisions were re-homed. Re-verified at HEAD 2026-09-14: `leading_slug` still tests exactly three ASCII digits plus a hyphen and its own doc comment still says so, so it still disagrees with `parse_feature_dir` in both directions. — `runtime/src/primitives/derive_dependencies.rs:189` (captured during review of 051-branch-scoped-spec-numbering)
- [ ] bug: 050's retired-filename sweep (`510eb25c`) inverted at least two legacy-describing records, and 9 further candidates are unread. **The shape**: the sweep substituted the current name into a sentence whose subject is the *retired* state, so the sentence still parses and asserts the opposite of what it recorded. `AGENTS.md` §Gotchas prescribes grepping a sweep diff for `X → X`; that cannot see this, because the two sides genuinely differ. **Two confirmed and fixed 2026-09-14**, both during 048's backfill pass: `specs/048-govern-acquired-runtime/scenarios/retired-namespace-tools-are-off-limits.md` read *a pre-`.ductus/` binary resolves `.ductus/`* (self-contradictory; the shipped `framework/bootstrap/ductus.md` still read `.govern/` and was correct throughout), and `specs/026-framework-self-audit/scenarios/family-23-sweep-target-manifest-parity.md` read *`.ductus/scripts/lib/specs-root.sh` kept resolving `.ductus/config.toml` then the legacy root with no `.ductus/` tier* — inverting the incident the scenario exists to record, which `AGENTS.md` carries correctly. **How the second was found is the durable half**: fixing the first removed one substitution from the sweep, which costs `mechanical_sweep`'s **repo-wide** uniformity test, and Family 19 immediately reported 026's review stale — a true signal the exemption had been masking. Proven by probe (Family 19 exits 0 at `9188d865`, 1 at `8d660cb3` in a detached worktree). Restoring 026's line returned it to `9dfa1299`, exactly its recorded `reviewed-digest`, so the gate went green with no review re-run and no claimed pass; both repairs are mechanical under [§spec-lifecycle](../framework/constitution.md#spec-lifecycle)'s *sweep residue where a substitution landed in a sentence it did not fit*, so neither spec left `done` for it. **Measured, not estimated**: `510eb25c` made 319 substitutions across 60 files; an aligned per-file diff filtered to pairs that removed the *last* retired token from a sentence carrying a legacy marker (`kept resolving`, `used to`, `pre-0N`, `legacy root`, `historical root`, `no longer exists`, `was the pre-`) yields **11 candidates across 8 files** — the two above plus 9 unread in `specs/005-workflows/spec.md` (now consolidated into 043), `specs/017-derive-dont-ask/{spec.md,data-model.md}`, `specs/022-deterministic-runtime/data-model.md` (its *historical roots* bullet is the strongest remaining candidate), `specs/027-bootstrap-migration-registry/spec.md` (three, where the dropped text is a `**superseded naming**` annotation rather than an inversion — possibly 050 acting deliberately), and `specs/029-bootstrap-runtime-autowire/scenarios/project-inputs-asked-once.md`. Each needs reading in full context; the filter is a candidate generator, not a verdict. **And the hunt is scoped too narrowly: 049's `9da4a7ae` produced the same shape and has never been run through the generator.** Found 2026-09-14 in 026's pass — `scenarios/host-namespace-parity.md` records a mismatch between the resolved namespace and the installed one, and 049 mapped **both** `govern` and `gov` to `ductus`, collapsing the two sides onto one name so the sentence asserted that `/ductus:*` names a namespace that does not exist while `.claude/commands/ductus/` sat beside it. Repaired in that pass. The generalisation, worth more than the instance: **a many-to-one substitution table is the tell** — a sweep that sends two distinct old tokens to one new token can flatten any sentence whose point is that the two differed, and the `X → X` grep cannot see it because neither side is unchanged. Re-run the generator over `9da4a7ae` as well as `510eb25c` before calling this item measured. **Cost by destination, which is what sets this**: every remaining candidate sits in a spec the corpus has already closed, so the two-disposal rule in `AGENTS.md` §Workflow binds — and four of the eight files are durable contracts (`data-model.md` / `scenarios/*.md`), each costing a reopen **plus a full five-pass re-review**, with 022 the corpus's largest. The `spec.md` hits reopen their spec but stale no review. Route with the other 022-blocked items rather than singly. **Not a mechanical sweep to run blind**: the correct repair differs per line, and the wrong one destroys the record a second time. **Routed 2026-09-14, and the unread count is 8, not 9** — one of the named candidate files, `specs/005-workflows/spec.md`, no longer exists, because 005 was consolidated into 043 on 2026-09-13; verified absent rather than assumed. **Disposal per destination, under the two-disposal rule in `AGENTS.md` §Workflow**: the strongest remaining candidate is `022/data-model.md`'s *historical roots* bullet, so it rides the single batched 022 reopen at no marginal cost; `017/spec.md` and `027/spec.md` (three) reopen their spec but stale **no** review, so each is a reopen plus an analyze; and `017/data-model.md` plus `029/scenarios/project-inputs-asked-once.md` are durable contracts costing a reopen **plus a full five-pass re-review** each, which is what makes them worth sequencing against any other reason to open those two specs. **The half most likely to be lost, so it is stated last and plainly: the generator has still never been run over `9da4a7ae`.** 049's sweep produced a confirmed instance of this exact shape and is a **many-to-one** substitution table (`govern` and `gov` both to `ductus`), which is the tell — a sweep that sends two distinct old tokens to one new token can flatten any sentence whose point is that the two differed, and neither the `X → X` grep nor this item's own filter sees it. Run the generator over `9da4a7ae` as well as `510eb25c` before calling this item measured (captured during /ductus:review of 048-govern-acquired-runtime)
